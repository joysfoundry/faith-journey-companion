# ACTS stories — board

Local, Git-tracked story board for **faith-journey-companion**. Not wired to Jira —
this table (plus the per-story files) **is** the tracker. Each chat is tied to one
story. Pattern follows `~/.claude/commands/WORKFLOW.md` with every Jira step skipped
and `ACTS` used wherever the kit says `{{PROJECT_KEY}}`.

- **IDs:** `ACTS-<n>`, next number from [`.counter`](.counter) (last-used number; increment on use).
- **Pointer:** `stories/ACTS-<n>.md` — one per story (goal, status, acceptance criteria).
- **Session handoffs:** `stories/ACTS-<n>/session-NN.md` — one per working session.
- **Template:** [`_TEMPLATE.md`](_TEMPLATE.md).
- **Config:** [`.claude/workflow.json`](../.claude/workflow.json).

## Board

Newest / active on top. `active` view = hide `Done`. This table is the source of truth for
active work — keep it in sync when a pointer changes. The **full numbered ledger** of all
75 stories (oldest-first, commits logged, EPIC column) lives in
[`docs/JIRA-BACKLOG.md`](../docs/JIRA-BACKLOG.md).

| ID | EPIC | Title | Status | Depends on | Next step | Updated |
|----|------|-------|--------|-----------|-----------|---------|
| [ACTS-184](ACTS-184.md) |  | Reflection composer — save/reflect quotes + layout reshape | Done | ACTS-181 | _Done 2026-09-09._ Split from ACTS-181. Composer **"Add a quote"** (mirrors library add, saves a real quote + links via `learning`); **reflect-from-quote** fixed (`Knowledge`→`learning`, snippet labels, `scripture_ref` detail); **"Save as quote"** on passage cards; **"Open dialogue" removed**; layout reshaped (title→text→add-icons→inspiration cards→themes→save), redundant chips dropped (cards carry ✕), theme chips moved beside Suggested. Commits `415250f`/`ee23eab` (shared w/ ACTS-181, unpushed at close). Relates ACTS-103/108/136/138 | 2026-09-09 |
| [ACTS-183](ACTS-183.md) |  | Tie a quote to its source content (any content — book, podcast, article…) | In Progress | ACTS-181 | _New (JC), filed 2026-09-09._ Spun off ACTS-181. A quote's `source` is free text today; tie it to **any** content `KnowledgeItem` (book, podcast, video, article, post) via a generic `source_item_id`. Add a **content picker** (existing items → inherit title/author + link) and an **"Add as content"** action (mint the item, then link). **Any** content's detail page lists **"Quotes from this."** Link is orthogonal to ACTS-181's `quote_kind` (a podcast quote = kind `open` + a link). Data-shape (flag): new generic `source_item_id?`, migrate w/ no `STORAGE_KEY` bump. Open Qs: one picker vs category-scoped?; overwrite vs fill-if-empty author?; reverse "add a quote from this"? Relates ACTS-176/180/179/171/178 | 2026-09-09 |
| [ACTS-182](ACTS-182.md) |  | Collapse the Home Vessels card and move it last | To Do | — | _New (JC), filed 2026-09-09._ On Home (`index.tsx`), make the **Vessels** card (D, ~L872) **collapsible + collapsed by default** and render it **last** in the stack (below Reflection). `SectionCard` has no collapse today; a `Collapsible` primitive is already available. Open Qs: persist collapse state?; "last" above or below Reflection?; add collapse as a reusable `SectionCard` option? Relates ACTS-137/177/179 | 2026-09-09 |
| [ACTS-181](ACTS-181.md) |  | Keepable quotes — typed quotes + Scripture citation & Bible deep-link | Done | — | _Done 2026-09-09._ Typed keepable quotes: kind chooser (Heard/Scripture/Book/Article) across add + edit + display; Scripture = passage + citation with byline "— Lk 1:26–38" + Bible deep-link (`buildPassageUrl`); book/article name the work in `source`, Vessel = who. New `KnowledgeItem` fields `quote_kind` + `scripture_ref`, migrated w/ no `STORAGE_KEY` bump. Reflection-composer side split to **ACTS-184**; book↔book linking to **ACTS-183**. Commits `8bf79ff`/`89ed4c4`/`415250f`/`ee23eab` (unpushed at close). Relates ACTS-108/156/176/179 | 2026-09-09 |
| [ACTS-180](ACTS-180.md) |  | Content links by format — paper book vs audio book (typed links) | To Do | — | _New (JC), filed 2026-09-09._ A book's links are really **formats**: a **paper** link (Amazon) and an **audio** link (Audible/Apple/Spotify). Add a format distinction to `KnowledgeLink` (new `format` field vs infer from platform — data-shape decision, flag first), let the editor add/remove links per format, and render format-aware icons/labels (book vs headphones) across library rows + Home pins via `platform-icon.tsx`. Migrate existing links with no `STORAGE_KEY` bump (like ACTS-177's `favorite→pinned`). Relates ACTS-137/178/171 | 2026-09-09 |
| [ACTS-179](ACTS-179.md) |  | Vessels library — add Voice and Channel as filters | Done | — | _New (JC), filed 2026-09-09._ Add **Voice** and **Channel** filters to the Vessels library (`formation.tsx`) alongside the existing All/Programs/Books/Media/Quotes/By-Vessel chips — narrow by a Voice (or kind: individual/org/ministry) and by channel platform (YouTube/Website/Podcast/…). Read-side only; pairs with ACTS-178's consistent Voice/Channel shape. Open Qs for JC: pick-a-voice vs kind-toggle; list-present-platforms vs all `LinkPlatform`. Relates ACTS-137/171/177/178 | 2026-09-09 |
| [ACTS-178](ACTS-178.md) |  | Paste-a-link — keep the person distinct from their channel; make kind correctable | Done | — | ✅ **Done + browser-verified** ([session-01](ACTS-178/session-01.md)), code `c6c5224`+`ca7d15a` (JC pushes). Adds @handle default for From + platform-icon convention (channels/pins) + grouped Home vessel rows + "Book" descriptor. Spun off ACTS-180 (paper/audio book link formats). **Look Up rebuilt:** Replaced the "New vessel / No vessel (General)" dropdown with a **"From"** section: **Name** (person or org) + **Individual/Organization** toggle (kind now chosen at import, not forced) + a quiet **"Save to General instead"** link (and "Attribute to someone" back). **Channel wired to the account home** (`youtube.com/@anamunley`) — fixed the `author.profileUrl \|\| raw` bug that filed the video URL as a channel; no channel fabricated when the home can't be read. `QuickAddLink.tsx` only; `tsc` clean, no console errors. Left as intended defaults: Name still prefills the channel/show title (editable), and after-the-fact kind edits ride the existing VoiceEditor. _New (JC), filed 2026-09-09._ Paste-a-link "new vessel" mode conflates person, channel, and content: importing Ana Munley's YouTube video named the **Vessel** after the **show** ("AfterMass with Ana Munley") and forced `kind: individual` via `detectVoiceKind`, with **no field for the person's name**. Separate Vessel/person name from Channel name, make **kind selectable** (individual/org/ministry) and correctable individual→organization, and store an individual the same way no matter how entered. Also note the `channelUrl = author.profileUrl \|\| raw` gotcha (a video URL masquerading as a channel). No `STORAGE_KEY` bump unless a data-shape change is unavoidable (flag first). Relates ACTS-171/137/177/179 | 2026-09-09 |
| [ACTS-177](ACTS-177.md) |  | Unify "Pin to Home" — one verb, one icon, for content and channels | Done | — | ✅ **Done + browser-verified** ([session-01](ACTS-177/session-01.md)), code `48ce906` + docs `e1b0bd0` (JC pushes — env push fails). _New (JC), filed 2026-09-09._ Two mechanisms put things on Home — a 📌 pushpin (`pinned`) on content and a ☆ star (`favorite`) on channels/links — same intent, two verbs, two icons. JC hit this in Vessels ("I can only pin the imported video not the channel"). **Decision (JC): TRUE unification** — collapse `favorite` + `pinned` into one `pinned` concept end to end (types → store/mutations → `pinnedLinks` → UI), one **"Pin to Home"** + pushpin control, ☆ gone. Data-shape change **JC-approved**, done via a **load-time `favorite`→`pinned` migration** in `loadDatabase` (no `STORAGE_KEY` bump, no reset, no pin lost). Surfaces: `formation.tsx` (ChannelChips/ContentRow), `voice.$voiceId.tsx`, `index.tsx` (Home `pinnedLinks`), `app-store-provider.tsx` toggles, `types.ts`, `seed.ts`. Relates ACTS-137/171/178/179 | 2026-09-09 |
| [ACTS-176](ACTS-176.md) |  | Mysteries tab mislabels the Mater Dei version as "USCCB — Scripture" | Done | — | ✅ Display-only bug: `mysteryVersions` named a version by its first mystery's source, so Mater Dei (which borrows USCCB Scripture for its 15 non-Glorious placeholders) showed as a duplicate "USCCB — Scripture". Now names by the version **label** first, matching `allMysteryBodies` + the devotion picker. `compiler.ts` one line; no `STORAGE_KEY` bump; browser-verified. Code `3076b24` pushed. **Live on next Publish.** | 2026-09-09 |
| [ACTS-174](ACTS-174.md) |  | Home: show upcoming sessions (next 7 days) inline with their date | Done | — | ✅ Widened Home's Prayer & Devotion window to `[today, today+7]`; future rows show a `Sat, Sep 12`-style date inline, each plan once at its next occurrence. `index.tsx` only; browser-verified. **Publish from `main`.** | 2026-09-08 |
| [ACTS-173](ACTS-173.md) |  | Seed a standing daily Open Prayer on Home's Prayer & Devotion | Done | — | ✅ Seeded `plan-daily-open-prayer` (endless daily) + a `loadDatabase` catch-up that rolls an overdue endless-daily plan's date up to today. Browser-verified. **Publish from `main`.** | 2026-09-08 |
| [ACTS-172](ACTS-172.md) |  | Seed the Mater Dei parish Rosary — Weekly Rosary devotion + parish mystery body | Done | — | ✅ Seeded the devotion (35 steps) + a Mater Dei mystery body over all 20 mysteries (5 NABRE + 15 USCCB placeholders); `STORAGE_KEY` v40→v41. Also fixed the blank devotion-block step ("see 32"). Browser-verified. **Publish; later fill 4 prose + swap USCCB placeholders.** | 2026-09-08 |
| [ACTS-169](ACTS-169.md) |  | Purge journaled reflections from follow-along links shared before the allowlist fix | To Do | — | _Privacy follow-up found while building ACTS-108._ `toShareItem` copied a session item's whole `configuration` into the shared payload, so a **journaled reflection** (`response`) travelled with any follow-along link. **Fixed going forward** (allowlist, 2026-09-07) — this is the links already out there: audit + purge `shared_sessions` payloads (re-encode through the same allowlist), decide what to tell sharers about **fragment** links that cannot be recalled, and add a regression guard. Relates ACTS-94/108/156 | 2026-09-07 |
| [ACTS-168](ACTS-168.md) |  | The mark as the O — a wordmark lockup, built and wired but switched off | Done | — | 🔧 **In progress — built, wired, and switched OFF; JC's decision is the only thing left.** _New (JC), filed 2026-09-06._ JC asked for the mark set **inside** the word as its O (it was drawn as "the O of Oravia" in ACTS-148), then: do both call sites, keep the design-system card **proposed**, sync it, **but do not render it in the app yet**. So: `OraviaWordmark.tsx` carries the lockup plus one flag, `WORDMARK_LOCKUP_ENABLED`, shipping **`false`**; `Brand.tsx` (mobile header + desktop side rail + mobile drawer, three renders) and `gate-shell.tsx` (beta gate, name prompt, both onboarding questions) each branch on it, so one line switches every surface together. **`gate-shell` moved with `Brand` deliberately** — it is the *largest* the name appears anywhere, the first thing a new person sees, and it has **never carried the mark at all**, so switching only the header would leave the biggest wordmark plain; turning the flag on **adds** the mark there rather than rearranging it. **Two constants were measured, not guessed:** `O_INK = 0.648` (a Cormorant capital O inks 0.648 of its font-size — rasterised and bounding-boxed at 128 and 256px, identical at weights 400/500/600; below ~40px the measurement is rasterisation noise, hence a constant not a runtime probe) and `OUTER = 72` (**SVG centres a stroke on its path**, so the visible outer diameter is `2×(r+stroke/2)` = 72 of the 100-unit canvas, **not** `2r` = 66 — the same off-by-a-stroke as ACTS-167, which here renders the ring ~9% small). Cross paths extracted to `REGULAR_CROSS`/`SMALL_CROSS` on `OraviaMark.tsx` so the wordmark cannot drift from the mark. Specimen card at `brand/wordmark.html` — fit ladder, cross vs ring-only, colourways, tagline lockup, small-size ladder, side-by-side with today's header, do/don't rules — marked **Proposed, not adopted**, the same convention `palette-states.html` uses for the staged palette. ⚠️ **Its `@dsCard` marker was first written `section`/`title`/`order` instead of `group`/`name`/`subtitle` and would have been silently skipped by the indexer** — check the marker against a working card, the failure is invisible. **Synced to claude.ai/design** (Oravia Design System, 23 cards) along with ACTS-167's corrected `mark.html` and regenerated icons, which had been sitting at the 2026-09-04 version. ✅ Verified both ways against the running dev server: flag off → plain `Oravia` h1, zero SVG, header unchanged; flag on → both surfaces switch, ring 22.0px measured against 22.0 expected at 36px, mark resolves `--gold`, word `--foreground`, and the gate `h1` still computes an accessible name of **"Oravia"** (role=img + aria-label) rather than "ravia". `tsc --noEmit` clean both ways. ⚠️ **The integration test that matters** is the one asserting the mark SVG is absent while the flag is off — that is what stops this shipping by accident; blocked on the ACTS-92 harness. ✅ **JC reviewed all four surfaces and said on** — flag flipped in `266a008`. ⚠️ **The one real tradeoff, recorded at the flag:** as a standalone mark the header ring was **21.6px** (a 30px `OraviaMark` beside 20px text, 1.5× the type, free to be any size); bound to the type as its O it is **13.3px** at the same font-size — **62% of what it was**. Inherent to the idea, not a defect. If the header needs its presence back, raise that surface's `fontSize`, **do not touch `FIT`** — the ratio is what keeps the ring reading as a letter rather than an ornament beside one. The arrival screen runs the other way: it gains a mark it never had, at 36px. **Header bumped to 24px** (`LOCKUP_SIZE`, `21d41c6`): 20px put the ring at 13.3px against the standalone mark's 21.6px (62%); 24px gives **16.0px, 74%** — as far as the row goes before the tagline stops sitting comfortably. Verified at 375px: right edge 204 of 375, no overflow, no collision with the date. ✅ **DONE 2026-09-06 — JC saw it live and said no.** Flag back to `false` (`3478d35`); the 24px bump reached only 74% and did not justify the lockup. Component, both wired call sites, `LOCKUP_SIZE` and the card all stay — `true` restores it in one edit, and **Proposed, not adopted** is accurate again ([session-01](ACTS-168/session-01.md)). Commits `789f055` (code) + `b15548a` (docs) — **committed, not pushed** (`git push` fails here: `could not read Username`), so JC pushes. Relates ACTS-148/167/144 | 2026-09-06 |
| [ACTS-167](ACTS-167.md) |  | Make the app-icon mark fill its tile — it shipped at 40% and reads as a dot | Done | — | 🔧 **In progress — code complete, awaiting JC's device check.** _New (JC), filed 2026-09-06._ JC screenshotted his iPhone home screen: the Oravia mark sits visibly smaller in its tile than the marks beside it. **Two causes compounding.** (1) The mark's 100-unit canvas has air baked in — the ring is `r33`, so the visible mark is only **66 of 100 units**. ACTS-148 scaled that *whole canvas* to the tile, landing the mark at **40% of the tile** (72px of 180; 202 of 512) and the maskable at **30.5%**. Nothing looks wrong in isolation — only next to other apps. (2) `apple-touch-icon.png` had **transparent corners**; iOS applies its own squircle and composites transparency onto black, so the navy tile was inset too. **Fix: size the tile from the ring, not the canvas** — `canvas = tile × ring_frac × 100 / (2r)`, with `ring_frac` **0.66** for the plain icons (which *is* the brand's own clear-space rule — a quarter-diameter each side — so the mark goes as large as the guideline allows and no larger) and **0.62** for the maskable (inside Android's 80% safe circle; the mark is circular, so nothing crops). `apple-touch-icon` + maskable now full-bleed; the `purpose:"any"` PNGs keep 20% corners. `favicon.ico` gained the documented **16/24/32/48/64** frames (it had only a 16), small cut below 20px. **New `docs/brand/make_icons.py`** — the ACTS-148 generator was never committed, so the icons had no reproducible source; it writes both `public/` and the design-system assets and resolves from `__file__` (its siblings all assume `cwd = docs/brand`; `make_og.py` in fact **dies** from the repo root — pre-existing, `og-cover.png` untouched). ⚠️ **Icons don't ship by replacing files** — `sw.js` serves images stale-while-revalidate and precaches `/icon-192.png`, so `VERSION` `fj-v2` → **`fj-v3`** and every `?v=` → `v=3` (`__root.tsx`, `manifest.webmanifest`, `invite.html`, the shell list). Same trap as ACTS-148. Verified by **pixel measurement** (ring/tile ratio, centring, corner alpha, `.ico` frames) + an iOS-squircle before/after render; `tsc --noEmit` clean; `make_about.py`/`make_flyer.py` re-run clean against the refactored `mark.py`. ⚠️ Forked off an **ACTS-162** chat — `ACTS-162.md`, `about.tsx`, `0003_feedback.sql` were dirty beforehand and are **not** staged; `invite.html` held both that session's copy edit and this `?v=` bump, so **only the `?v=` hunk is committed**. ✅ **DONE 2026-09-06 — verified against the live deploy:** all five icons byte-identical to the repo, live `sw.js` on `fj-v3` ([session-01](ACTS-167/session-01.md)). ⚠️ **iOS snapshots a home-screen icon when the site is added**, so republishing does not update one already there — it must be deleted and re-added from Safari. JC was told, closed without reporting back; recorded, not left open. Commits `164a522` (code) + `e8b9334` (docs) — **committed, not pushed** (`git push` fails here: `could not read Username`), so JC pushes. Relates ACTS-148/144/96 | 2026-09-06 |
| [ACTS-166](ACTS-166.md) |  | Make the Home daily-rosary switch actually switch — one picker for devotions and prayer apps | Done | — | ✅ **DONE 2026-09-05 + browser-verified** ([session-01](ACTS-166/session-01.md)). _New (JC), filed 2026-09-05._ JC: from Home → Prayer block → Daily Rosary, the switch icon opens the "Daily devotion" picker but choosing a devotion leaves the row on **Hallow**. **Cause: a precedence bug, not a broken write.** The row is decided by `daily_rosary_mode` (`index.tsx:345`) — while it is `"external"` the subtitle is "Opens in Hallow" and the action is the external link — but the picker only ever wrote `daily_template_id`, which **nothing on that row reads in external mode**. Settings dodges the same trap only by hiding its devotion select entirely while external. JC's opening idea was to link the picker out to Settings; the smaller fix was to make the switch *switch*, and JC then asked for the apps to be **in the list as well** and to **match what the Settings list has**. Result: one dialog, two groups — **Pray here** (Standard Holy Rosary + devotions; choosing one also sets `daily_rosary_mode: "app"`, the actual fix) and **Open in another app** (all of `PRAYER_APPS` in Settings' order, names and blurbs included). Checkmark tracks the **live** choice — the app while external, the devotion otherwise. `Another app or website` is the one row that still needs Settings (nowhere in a dialog to paste a URL), so it sets the mode and navigates to `/settings` — JC's original idea kept where it earns its place; with a URL saved it shows the domain instead. ⚠️ **The missing regression test is the follow-on** (integration: render Home external, pick a devotion, assert the row leaves Hallow) — blocked on the ACTS-92 harness. ⚠️ **Port-8080 gotcha again** — `.claude/launch.json` pins 8080, a stale server still holds it, vite fell back to **8081** and the harness tab was dead until repointed by hand. ⚠️ **A hidden Browser pane** makes `computer` scroll/hover time out while `screenshot`/`javascript_tool`/`get_page_text` keep working — drive dialogs with `javascript_tool` clicks. ⚠️ `stories/ACTS-162.md` was **already dirty before this session** (concurrent session) and was left unstaged, not absorbed. One route file; no lib logic, no data shape, no `STORAGE_KEY` bump. `tsc` clean. ✅ **JC-verified in the app and closed 2026-09-05.** Commits `8da86e2` (code) + `74acf68` (docs) **pushed by JC**; the close commit `a265367` was pushed by JC within minutes. ⚠️ **That promptness broke something:** this session then `--amend`ed the already-pushed `a265367` into `c76380e` to fix a wrong push claim, diverging local from remote and handing JC three conflicted files on the next `git pull` (this row's file among them). **Never amend in this repo — JC pushes fast and Lovable writes straight to `main`; correct with a follow-up commit.** ⚠️ `git push` still fails here (`could not read Username`) — seeing the earlier commits on `origin/main` briefly suggested the error was cosmetic, but it is not: JC had pushed in between. Confirm with `git log @{u}..HEAD`, don't infer from the error or the remote ref. **Port 8080 is clear again** — by the time JC asked to kill the holder, nothing held it (no listener, no vite process, empty preview registry); it died with a session restart and a fresh `preview_start` bound 8080 with no fallback. The holder is always a dev server from an earlier *Claude session*, not a system service. Relates ACTS-153/99/92 | 2026-09-05 |
| [ACTS-165](ACTS-165.md) |  | Swap the About sections — prayer/devotions under "More than a prayer app", learning + fellowship under "One place for your journey" | Done | — | ✅ **DONE 2026-09-05 + browser-verified** ([session-01](ACTS-165/session-01.md)), commits `d7c4c37` + `0b49207`, pushed by JC. Straight after ACTS-164, JC found the two middle About sections holding each other's content: **"More than a prayer app"** carried the faith-*learning* paragraph while **"One place for your journey"** carried all three prayer/devotion paragraphs. Swapped, and each rewritten to make its own claim. **More than a prayer app** now opens on prayer taking more than one shape and Oravia **honoring both** open and structured prayer, then states the value prop as a **contrast** rather than a category label: not that the app stores prayers, but that it **digitizes them into one place**, which is what lets you **customize a devotion the way you, your family, or your parish pray it** — compiled into a guided flow, and shareable. **One place for your journey** now carries the learning + hub paragraphs, **elevated past "good for me"** — one place is easy to hand on, so it encourages fellowship; closes on "the people walking with you are part of your journey too." **Redundancy removed:** ACTS-164 said "scattered → gathered" twice; the scattered-sources list ("a Rosary pamphlet, a hymnal, a holy card, a family novena someone texted you") is **gone for good** (JC) and the sentence tightened rather than left a stub. ⚠️ **Two good lines were nearly lost:** the guided-flow clause ("the right day, the right mysteries and readings, reflection as a first-class step, and a way to sing") and **"so the tool disappears and the prayer stays"** existed in `about.tsx` **only** — `invite.html` never had them — so a swap driven by the invite text dropped them silently. Restored, and now in **both** files. **The mirrors were not byte-identical before this story: diff them, don't assume.** ⚠️ **`find` gave a false negative** — searching the rendered page for "the tool disappears and the prayer stays" returned no matches while the sentence was on screen (it matches **truncated** accessibility-tree lines). JC saw the same absence independently. **Use `get_page_text` to confirm copy; a `find` miss is no evidence, not absence.** **"build" → "customize" + personal → communal:** JC's first wording ("the way your family or parish pray it") left the following list ("your prayers, in your order, with your intentions") pulling the other way; flagged, and JC chose naming all three. Copy only — no logic, no data shape, no `STORAGE_KEY` bump. `tsc` + `vite build` clean; prettier warnings pre-existing (ACTS-164). ⚠️ `git push` fails from this environment (no credentials) — JC pushed. ⚠️ A **concurrent session held ACTS-162 open** and edited the board + ledger at 15:56; this story's rows could not be added without also staging that status flip, so the close commit carries it, stated plainly. **Left open:** ACTS-147 (PRD resync) is further out of date — the About page's central claim changed twice today. Relates ACTS-164/143/160/147 | 2026-09-05 |
| [ACTS-164](ACTS-164.md) |  | Say the "build it once, share it, pray alongside" promise on the About page | Done | — | ✅ **DONE 2026-09-05 + browser-verified** ([session-01](ACTS-164/session-01.md)), commits `d5da07d` + `3bead83`, pushed by JC. _New (JC), filed 2026-09-05._ The About copy already gestures at this — "compiling a devotion into a guided flow … and a follow link so others can pray along" — but it is buried mid-sentence in a list of capabilities. JC asked for it plainly: build a devotion **as you have learned to pray it**, without flipping through booklets and papers, and **share it** so others pray alongside you — so someone still learning is not lost and can stay focused on the prayer. ⚠️ **The copy lives in two mirrored places** and must not drift: `src/routes/about.tsx` (in-app, ACTS-143) and `public/invite.html` (the public invitation, outside the gate, ACTS-160). Both carry a "One place for your journey" section — that is where it goes. Two sentences, existing voice, no new heading. Copy only: no logic, no data shape, no `STORAGE_KEY` bump. **Two judgment calls at the edit:** the paragraph above already ended "…a way to sing, **and a follow link so others can pray along**" — the same promise buried mid-list, which is *why* the story existed — so the clause was **removed** rather than said twice; and the two surfaces keep **different contractions on purpose** (`invite.html` avoids them throughout — "and it **is** there every time" — while the in-app page keeps "it&rsquo;s"), so do not "fix" that into a diff when re-syncing. ⚠️ **Verification needed a real browser:** `/about` is client-rendered, so `curl` returns a 4 KB shell with none of the copy — and the preview harness could not reach the dev server because `.claude/launch.json` pins `port: 8080` while a **stale vite server from an earlier session held 8080**, pushing vite to 8081; opened the stale server directly with `preview_start {url}` instead. Local beta gate bypassed via the `acts-beta-unlocked-v1` localStorage key. `tsc` + `vite build` clean; prettier warnings in both files are **pre-existing** (confirmed against a stashed clean tree, left untouched per ACTS-155). ⚠️ `git push` fails from this environment (no credentials) — JC pushed. Relates ACTS-143/160/94/147 | 2026-09-05 |
| [ACTS-163](ACTS-163.md) |  | Guided tour — numbered hotspots that explain the screen | To Do | — | _New (JC), filed 2026-09-05._ A tester's first look at Today is a screen of unfamiliar icons. Build an overlay they turn **on**: each explainable control wears a small numbered dot, tapping one opens a short description anchored to that control, tapping away closes it. Deliberately **not** a step-through carousel — the dot form lets someone read the two things that confused them and leave. Targets opt in with `data-tour="<id>"` on the **real element** and copy lives in one data file, so a renamed or moved control cannot drift out of sync with its explanation. Entry points: a menu item plus a single post-onboarding offer, stamped like ACTS-153 so it never nags. Start with Today; other screens reuse the same overlay. ⚠️ The ACTS-153 same-shaped-steps React gotcha applies — key each popover by step id. **On "can Claude make a video tutorial?"** — no narrated screen recording (no capture or voice tool); a silent GIF walkthrough of the real app **is** possible; an MP4 from scripted screenshots is possible but `ffmpeg` isn't installed here. Recommend the overlay anyway: a recording is a photograph of a UI that changes weekly and goes wrong silently, it's a heavy cellular download, and it can't be read without sound. Where a GIF **does** win is *outside* the app — on the ACTS-160 invitation page, showing someone what Oravia is before they have it. Relates ACTS-153/160/162 | 2026-09-05 |
| [ACTS-162](ACTS-162.md) |  | Send feedback from the menu | In Progress | — | _New (JC), filed 2026-09-05._ Feedback is the point of the beta and the only channel today is "text JC", which loses everything a tester notices but doesn't think worth a message. Add **Send feedback** to `secondaryNavLinks` (so it reaches the desktop rail and mobile drawer at once) → a `/feedback` route with a message + kind (bug/idea/other). **Channel = table, not email or Slack**, and the reason is concrete: the browser can't send mail or hit a webhook without a secret, and any secret here ships in the bundle — same reason the beta passcode was never a security control. Build it exactly like ACTS-94 **after its lockdown**: `public.feedback` with RLS on and **no policies**, revoked from anon/authenticated, granted to `service_role`, reached only through a `createServerFn` with zod validation and a hard size cap. Insert-only, so no one can enumerate anyone else's. This is a **third** deliberate path off-device in a local-first app and the first that sends something the person wrote about themselves — so the UI states plainly what leaves, and it carries **no** prayers, reflections or journey data (ACTS-157's export stays the path for those). "Combo" (webhook/Edge Function → email or Slack) is a **follow-on**: the table is what makes the ping possible, and shipping it first means nothing is lost meanwhile. ⚠️ Needs `SUPABASE_SERVICE_ROLE_KEY` locally, like share links — surface the failure, don't fake success. Relates ACTS-94/157/161/82 | 2026-09-05 |
| [ACTS-161](ACTS-161.md) |  | Stand down the private-beta passcode gate | Done | — | ✅ Shipped ([session-01](ACTS-161/session-01.md)), commits `bdae185` + `908a3ac` + `1ae212e`, pushed. ⚠️ **One step is JC's and it is the actual stand-down: clear `VITE_BETA_PASSCODE` in Lovable and redeploy.** "Remove the gate for now" — so the invitation is the only thing a friend has to act on. `beta-gate.tsx` runs three steps and only step 1 is a gate in the exclusionary sense; **it's already conditional**, skipped entirely when `VITE_BETA_PASSCODE` is empty, which is how we run locally every day. So: **route A (recommended) — clear the env var in Lovable and redeploy.** Zero code, reversible in one field, and "for now" says pause, not decision. Route B (delete the branch, `UNLOCK_KEY` and the env block) means rewriting it to re-arm. It was never security anyway — `VITE_`-prefixed, so it ships in the bundle — and the page recipients actually read (`public/invite.html`, ACTS-160) was never behind it. Bonus: ACTS-158 caught Apple screenshotting **the gate's own name prompt** into iMessage previews. **Scope:** the name prompt (step 2) and onboarding (step 3) **stay** — they personalise and configure, they don't keep anyone out; there's a checkbox in the pointer for JC if the name prompt should go too. Real accounts (ACTS-82/87/88) are what replace this properly. Relates ACTS-82/153/158/160 | 2026-09-05 |
| [ACTS-160](ACTS-160.md) |  | Serve the beta invitation from the app's own origin | Done | — | _New (JC), 2026-09-05._ ACTS-159 published the invitation as a Claude artifact, which raised a question it could not answer: **does a recipient need a Claude account to open it?** For friends and family invited to a devotional beta that is a hard stop, and a Claude-branded URL beside a prayer-app link reads oddly regardless — so serve it ourselves and the question disappears. Moved (`git mv`, **not copied** — no second version to drift, the failure ACTS-159 existed to fix) to **`public/invite.html`** → `myoravia.lovable.app/invite.html`. **Why `public/`:** files there are handed straight to the browser without going through the router, so the page sits **outside** the client-side beta gate — no passcode, no name prompt, no account; the gate still does its job when they tap through. Rewrapped as a standalone document (doctype/head/body — the artifact wrapper that supplied those is gone) and given **its own OG/Twitter tags**, so sharing *this* link shows the ACTS-158 card instead of falling back to a page screenshot. Named `/invite.html` rather than `/about.html` because the app already has a gated in-app `/about`. Verified: 200 `text/html`, no app bundle, no gate markup, renders at 375×812. The artifact is superseded but left private and harmless — note its file path moved, so the old URL will not update itself.  ✅ **DONE 2026-09-05** ([session-01](ACTS-160/session-01.md)) — **closed on JC's live check**; browser navigation to external domains is blocked in the Claude Code environment, so this session verified locally only (200 `text/html`, no app bundle, no gate markup, renders at 375×812). Related but deliberately separate: the invitation says "no account needed" while **ACTS-161** (stand down the passcode gate) is still To Do — JC's call is that the published state is fine as it stands and the gate comes down soon. Relates ACTS-158/159/82 | 2026-09-05 |
| [ACTS-159](ACTS-159.md) |  | Refresh or retire the published Oravia brand artifact | Done | — | _Found while shipping ACTS-158; **JC: capture, don't fix now.**_ The published brand/About artifact — **<https://claude.ai/code/artifact/03d0869e-d9ac-40a3-b7ba-ce903b54de62>** ("Oravia", 🕯️, private, updated 2026-09-04, source [`docs/brand/oravia-brand.html`](../docs/brand/oravia-brand.html)) — has drifted three ways: it still draws the **old flame-and-open-O mark** instead of the ACTS-148 cross-in-compass, it carries **no beta link at all** (so it never had the dead `www.` host ACTS-158 fixed — nothing to correct there), and its copy predates the later About trims. Private and only ever hand-shared, so nothing is publicly wrong; the risk is a link already sent to a tester resolving to the old identity. **Open: refresh or retire** — the two PNGs and the ACTS-158 share card now do the same job, so a fourth surface with the same copy may not be worth maintaining; if refreshed, publish to the **same URL** so existing links keep working, and consider generating it beside the PNGs rather than hand-maintaining HTML that drifts again.  **DECIDED 2026-09-05: RETIRE, not refresh.** A replacement was built and published while closing ACTS-158 — **<https://claude.ai/code/artifact/0a832e6c-efa7-4cea-91bb-5503fee85a41>** ("Oravia Beta Invitation", 🧭; source [`docs/brand/oravia-about.html`](../docs/brand/oravia-about.html)) — the web twin of `oravia-about.png` with the current mark, current copy, the palette lifted verbatim from `make_about.py`, and the beta link as a real tappable `<a>`. **HTML over .docx** because the piece is sent to phones: it reflows (the 1600px PNG needs pinch-zoom), the text is selectable and screen-reader accessible, the mark is inline SVG, and a `@media print` block makes ⌘P → Save as PDF produce the file version, so the docx need never exist. Retiring beat refreshing in place because the old artifact was private and hand-shared only (no public link breaks), and refreshing would have meant hand-maintaining a second HTML page with the same copy — the exact drift that created this story. **Left to settle:** `docs/brand/oravia-brand.html`, the retired artifact's source, is still in the repo carrying the old mark and pre-trim copy — delete or mark superseded in place. README now flags it RETIRED.  ✅ **DONE 2026-09-05** ([session-01](ACTS-159/session-01.md)), commits `9023026`, `503e3d3`, `7a24e12`, `dd6c398`. `oravia-brand.html` **deleted** (JC), dangling refs in `make_og.py` + README cleaned up; `design-system/brand/mark-at-size.html` still cites it and is left as the historical record of why the flame was retired. ⚠️ **Raised at close, not in scope:** where a recipient actually reaches a shared `claude.ai/code/artifact/…` link, and whether it needs a Claude account — if so, the page belongs on the app's own origin (a static file in `public/`, outside the client-side beta gate) instead. Relates ACTS-144/148/158 | 2026-09-05 |
| [ACTS-158](ACTS-158.md) |  | Social share preview card (Open Graph / Twitter) for the beta link | Done | — | _New (JC), filed 2026-09-05._ Sharing the beta link produced **no card at all**: nothing in the app set `og:image` — `__root.tsx` had title/description/theme-color and a `twitter:card` with no image to go with it, and the per-route heads added `og:title`/`og:description` but never an image, `og:url` or `og:site_name`. With nothing to find, **Apple screenshots the page instead** — and what it caught was the beta gate's name prompt, too small to read (confirmed from a real iMessage share). Now a 1200×630 card at `public/og-cover.png` — the ACTS-148 cross-in-compass mark, the name, the tagline, one line of about copy, and **"Free beta · No account needed · Your entries stay on your device"** — on the dark Marian-blue/gold palette from `oravia-brand.html`, so it reads as *something* in a feed of white rectangles. Generated by `docs/brand/make_og.py` (sibling of `make_about.py`/`make_flyer.py`, Pillow, 2× supersampled; the mark is drawn from the SVG's own 100-unit geometry at 8× and downsampled because Pillow's ellipse outline has no antialiasing). Tags live in the **root** route only, so **every** page inherits the card while child heads still override title/description by key — verified on `/`, `/about`, `/pray`, `/settings` against the **server-rendered** HTML, which is what a scraper actually reads. `og:image`/`og:url` are absolute (relative paths are dropped silently by every platform), built from `VITE_PUBLIC_URL` → `PUBLIC_URL` → the prod host, so a preview deploy can advertise itself. ⚠️ **Copying the supabase client's env pattern verbatim was a real bug:** `process` doesn't exist in the browser, and unlike that file's always-set `VITE_*` value, `VITE_PUBLIC_URL` is normally unset — so the `process.env` arm is the *usual* path, and `head()` runs on every client navigation. Unguarded it throws in the root route and blanks the app; now behind `typeof process !== "undefined"`. Image 200s at 102 KB; `tsc` + `eslint` + `vite build` clean. **Next: deploy, then re-share to confirm** — scrapers cache hard, hence `?v=1` on the image URL (bump on any redraw).  ✅ **DONE 2026-09-05 — confirmed live by JC** ([session-01](ACTS-158/session-01.md)). Grew past the original scope as JC reviewed: **"calm" removed** from every brand surface (card, `og:description`, root + Home descriptions, manifest) while deliberately kept in `seed.ts` (a Nativity reflection) and `themes.ts` (a **keyword** in the peace matcher — deleting it would break theme matching); the **dead `www.` host** corrected in code and on the printed collateral; the **current mark** put on the About card (which had none) and the flyer (still drawing the retired flame-and-open-O), extracted to one shared `docs/brand/mark.py` — `og-cover.png` regenerates **byte-identical** after the extraction; **no CTA on the card** (JC: "it works as it is"). ⚠️ **Second bug JC's tagline note exposed:** the index route's own `og:title`/`og:description` overrode the root's, so `/` — the URL people actually share — still read "Your daily prayer companion" and **silently dropped the "free beta, no account needed" line**, the whole point of the card. Relates ACTS-144/148/82 | 2026-09-05 |
| [ACTS-157](ACTS-157.md) |  | Export my journey for AI insights — menu item, built-in prompt, date range | Done | — | _New (JC), filed 2026-09-04._ No export of any kind exists today — the journey is readable one screen at a time only. Ship a **new secondary nav entry** (beside About/Settings, the ACTS-143 precedent) → an export screen that builds **one Markdown document** the user pastes into ChatGPT or Claude. **Decisions locked at filing:** contents = **all four layers** (reflections/journal + prayer sessions + intentions & Mass experiences + formation/knowledge); delivery = **both** Download `.md` **and** Copy to clipboard (clipboard is what works on a phone); range = **Everything (start → today)** by default or a **custom from/to**. The **prompt ships inside the document** so the user pastes one thing — what the data is, what to look for (recurring themes, movement over time, returning questions, what's gone quiet), plus the PRD guardrail: **name patterns and ask questions, never speak for God or hand down discernment**. Read-only, no `STORAGE_KEY` bump; markdown-lite marks from ACTS-156 pass straight through. ⚠️ **Deliberately not ACTS-113** (in-app grounded Insights → Wisdom, still To Do) — this is the near-term, zero-AI-in-the-app path to the same value. Open: prompt wording (needs a pass with JC), per-entity date key, oversize journals vs. chat context, and the privacy line (About promises "everything is local" — the file is built locally, but pasting it sends it). Closed by JC. ✅ **DONE 2026-09-04 + browser-verified** ([session-01](ACTS-157/session-01.md)), commits `8c85d7e`, `8521c31` — pushed. New `/export` route + **Export journey** nav entry (`FileDown`, next to Add prayers) → one Markdown document built by `src/lib/prayer/journeyExport.ts` (pure over `db`, read-only, **no `STORAGE_KEY` bump**). Four layers filtered by range, sections oldest→newest, links resolved through the existing `resolveInspiration` (ACTS-102/135) so no raw ids leak; ACTS-156 markdown-lite marks pass through untouched. **The prompt is editable in the app** (collapsed section + Reset), so its wording can be iterated by use. **Date key decided per layer and stated in the file**: Mass by its own `date`, a saved item by `start_date` else `created_at` (= when saved, not read). Size is shown ("About N words") and warned past 200k chars rather than capped; photos/audio/common-prayer-texts are **named as excluded, never silently dropped**; the privacy line sits right above the buttons — built on-device, but pasting hands it to that company's servers. ⚠️ **Found while verifying:** `quoteBody()` falls back to `title`, so every **book** printed its own title back as a blockquote — guarded with `isQuote()`; a quote's heading was also a 57-char snippet of the text below it, now just "Quote". New harness `scripts/verify-journey-export.ts` **47/47**; `tsc` + `vite build` clean; other three harnesses still green; zero lint errors. **Copy to clipboard needs a human tap** — the pane can't focus the document for `navigator.clipboard`; the download blob was captured and read back instead. **+ "Since last export" (JC, same session):** a third range option — everything since the export the user actually **took**, stamped in `settings.last_export_at` by Copy/Download only (never by opening the page), hidden until there is one, and leading by default when there is. The window opens **on** the day of that export, not the day after: ranges are date-granular, so exporting Thursday morning and journaling Thursday night would otherwise lose the entry — overlapping the day re-sends a few at most, and the screen says so. ⚠️ **Building it exposed a real bug in the first cut:** the stamp is UTC (`toISOString()`, like every `created_at` here) and the builder filed entries by `iso.slice(0,10)`, so an evening export opened the catch-up window **in the future** — the exact gap the overlap prevents. Underneath sat the general case: **every** layer was filed by its UTC day, so an 8pm-Pacific reflection filed under tomorrow, outside a range the user picked ending that day. `dayOf()` now converts to the **local** calendar day and leaves an already-plain date (`MassExperience.date`, `start_date`) as written instead of re-parsing it as UTC midnight — the trap `todayISO()` documents in `compiler.ts`. Harness pins `TZ`; `last_export_at` is additive, still no `STORAGE_KEY` bump. **+ anchored to the user's own entries (JC):** with an empty journal the label read **"Everything — 2023-12-31 to today"** — every seeded row carries `now = "2024-01-01T00:00:00.000Z"` (UTC midnight → Dec 31 Pacific), so a new user was told their journey began before they installed the app. That constant is now exported as **`SEED_EPOCH`** (one sentinel across all 48 pre-installed records; a real `new Date().toISOString()` can never equal it, so no id-prefix guessing) and `journeyDateBounds` counts **only the user's own rows** — an untouched install now reads "Everything — Your whole journey". Seeded library items are **kept, not dropped** (someone may really be working through the pre-installed Bible in a Year) but are **headed by name alone** — their only date is the install stamp — and **flagged "came with the app, not chosen"**, so an assistant can't read them as evidence of what this person was drawn to. Harness now **57/57**. **Left to try (not blocking):** one human tap on Copy (the pane can't focus the document for `navigator.clipboard`), JC's pass on the prompt wording — editable in the app precisely so it can be judged in use — and a real export actually run through an assistant. Whatever shape that reveals feeds **ACTS-113**, which this story deliberately left open. Relates ACTS-113/135/103/102/143 | 2026-09-04 |
| [ACTS-156](ACTS-156.md) |  | Formatting toolbar (bold / italic / underline) in the reflection box, hideable | Done | — | ✅ **DONE 2026-09-04 + browser-verified** ([session-01](ACTS-156/session-01.md)), commits `6ff9f71`, `e4d317a`, `d9abe31` — pushed. Formatting is **markdown-lite inside the same `body` string** (`**bold**`, `*italic*`, `<u>underline</u>`) — `body` stays a plain string, so **no data-shape change and no `STORAGE_KEY` bump** (store stayed on `…-db-v39`); the ACTS-94 share codec and the ACTS-136 shared draft carry formatted text without knowing about it. One shared `RichTextArea` (toolbar + textarea + ⌘/Ctrl+B/I/U) now serves **three** writing surfaces — the Home + `/reflections` composer, the journal entry's edit view, and the in-session Lectio card — and one `<FormattedText>` renders the marks on the Journal list, the sitting-group preview, the single-entry dialog and `ItemView`. It emits **React text nodes, never an HTML string**, so `<script>` in a body is displayed, not run (verified). Hide toggle persists in `settings.reflection_toolbar_hidden` (additive); shortcuts keep working while hidden. ⚠️ **The harness earned its keep:** the first parser turned "2 * 3 and 4 * 5" into italics and mangled `***x***` — which the toolbar itself produces on a second click. Fixed by matching asterisk **runs by exact length** plus CommonMark's flanking rule, so stray asterisks in prose stay literal. New unit harness `scripts/verify-richtext.ts` **33/33**; `tsc` + `vite build` clean; liturgical 19/19 and verify-merge still PASS; zero new lint errors. **Toolbar reworked after JC's first look:** the 32px bar above the field read as too much chrome, so the controls moved *inside* the textarea along its bottom edge at `size-7`/`size-3.5` — `pointer-events-none` on the strip so taps beside the buttons still reach the text, `pb-9` so a scrolled entry never runs under them, and the padding held constant so toggling never resizes the field. Hidden, only a faint corner toggle remains. Relates ACTS-136/135/103/102/139 | 2026-09-04 |
| [ACTS-155](ACTS-155.md) |  | Repair the stale `verify-merge.ts` harness script (dead Learn API) | Done | — | ✅ **DONE 2026-09-04** ([session-01](ACTS-155/session-01.md)), commit `0c38494`. `npx tsx scripts/verify-merge.ts` crashed on `mutations.addLearningItem is not a function` — the **Journey layer** block still called the pre-unification Learn API, never updated when Learn/Programs/Resources folded into `KnowledgeItem`. Ported `addLearningItem`→`addKnowledgeItem`, `setLearningStatus`→`setKnowledgeStatus`, `learning_items`→`knowledge_items`, `content_type`→`category`. Assertions **preserved, not rewritten**; the other six checks are **byte-identical** to a captured baseline (diffed). Now prints `knowledge=5 (seeded 4, know-x=finished)` and **PASS**. ⚠️ **Root cause:** `scripts/` was missing from `tsconfig.json` `include`, so `tsc` never saw the dead call — added it, which surfaced one real `noUncheckedIndexedAccess` hole (`j.reflections[0]`, now guarded). Both harness scripts green (liturgical 19/19), `tsc` clean, `vite build` clean, **zero new lint errors** (the file's 9 prettier errors are pre-existing and outside the touched block). Not ACTS-92 — stays plain `npx tsx`, no runner. Relates ACTS-92/91/154 | 2026-09-04 |
| [ACTS-154](ACTS-154.md) |  | Move the "Online Bible" link onto the Word card header | Done | — | ✅ **DONE 2026-09-04 + browser-verified** ([session-01](ACTS-154/session-01.md)), commit `35d8faa`. The link sat **inside** the Daily Readings block, smaller and beneath it, reading as a **child of the readings** when it really opens the reader's own Bible. Now a section-level header link via one exported `OnlineBibleLink` that resolves `resolveBibleHomeUrl` itself and returns `null` on `""`, so the omit-when-unset rule has one home. ⚠️ **The story's premise was half wrong:** `WordSection` is shared, but the header slot lives in the *callers* — only the removal was shared, the placement had to be done per surface (JC picked **option 1**). And `/word`'s `PageShell` `AppShell` **already had** an `action` prop (five pages use it), so option 1 needed **no layout change and no prop threading**. No second `BookOpen` — `WordSection` no longer imports it at all. Verified four states (set/unset × Home/`/word`), href resolves to `bible.com` with `noopener noreferrer`, exactly one such link per page, no wrap at 375px. `tsc` clean. Presentation only, no `STORAGE_KEY` bump. Follow-on → **ACTS-155**. Relates ACTS-153/152/145 | 2026-09-04 |
| [ACTS-153](ACTS-153.md) |  | First-launch onboarding — ask Bible app + Daily Rosary (defaults, changeable in Settings) | Done | — | ✅ **DONE 2026-09-04 + browser-verified** ([session-01](ACTS-153/session-01.md)). Two questions after the beta name step, in their own `onboarding.tsx` (frame extracted to `gate-shell.tsx`): **your Bible** (app + translation) and **your daily rosary** (in-app, or launch Hallow/another app — the ACTS-133 path). Both skippable; skipping writes only the stamp. **Decisions (JC):** everyone asked **once**, testers included → completion is a real field (`settings.onboarding_completed_at`, additive, no `STORAGE_KEY` bump); **`DEFAULT_TRANSLATION` NIV→NABRE** (Catholic canon, matches the USCCB readings the Word page links to — ⚠️ also moves anyone who never picked one); ACTS-144's parked voice lines placed ("Keep your seeking for God." under **Begin**). ⚠️ **Found + fixed:** same-shaped steps made React *reconcile* not remount → the Radix Select arrived empty and Begin silently wrote `external` + blank app id. Harness 11/11, `tsc` clean. Commits `d926b94`, `ea441e2`, `ceed2e6` — pushed. Follow-on → ACTS-154 | 2026-09-04 |
| [ACTS-152](ACTS-152.md) | ACTS-144 | Swap the Word and Vessels nav icons (open book, amphora) | Done | — | **DONE 2026-09-04.** Quick one: `Word` `Sun`→`BookOpen`, `Vessels` `Lightbulb`→`Amphora` (lucide's two-handled jar — water jug / oil vessel). Single edit in `src/components/layout/nav-links.ts`, which feeds the side rail, mobile drawer and bottom bar alike. `tsc` clean; both icons browser-verified in the running app. No `STORAGE_KEY` bump. Relates ACTS-148/144 | 2026-09-04 |
| [ACTS-147](ACTS-147.md) |  | Resync PRD v3 → v4 (post-rebrand) + fold in About-page framing | To Do | ACTS-144 | _New (JC)._ Next continuation of the PRD after **ACTS-117** delivered **v3** (the v8 rebase = the anchor/base). **Gated on the Oravia rebrand (ACTS-144) being locked** + a clean tree. Then `/prd-sync` v3→**v4**: refresh "What's shipped" (Lectio/Reflection arc 138–142, About 143, Home Vessels 145), apply Oravia name doc-wide, fold in the **About-page copy** ("sums up this MVP") as the vision/problem anchor, re-export `.docx`. Carries the ACTS-117 §25D open question. Relates ACTS-117/104/105/143 | 2026-09-04 |
| [ACTS-146](ACTS-146.md) |  | Install the Archify skill (dev tooling) — audited safe | Done | — | _New (JC), parked._ Install **Archify** ([`github.com/tt-a1i/archify`](https://github.com/tt-a1i/archify), MIT) for architecture/workflow/sequence/dataflow/lifecycle diagrams of the app. **Security-reviewed 2026-09-04 with `skill-auditor` → ✅ clean** (no install hooks; one disclosed GET-only version-ping to a hardcoded URL, no data sent, never auto-updates; no exfil/`eval`/persistence). Decide install **scope** (global `-g` vs project) and note the `npx skills add` **installer CLI** was not itself audited. Relates ACTS-104/105 | 2026-09-04 |
| [ACTS-149](ACTS-149.md) | ACTS-108 | ACTS guided prayer framework — open-prayer mode + info button (A·C·T·S) | To Do | — | _New (JC) — the "keep ACTS" follow-on from the Oravia rebrand (ACTS-144)._ Honor the ACTS acronym where it's true: structuring prayer. **(A)** an **info button** unfolding Adoration·Contrition·Thanksgiving·Supplication; **(B)** an **ACTS open-prayer mode** — four movements as sequenced open-prayer steps, riding the `open_prayer` kind (ACTS-108). ACTS becomes a feature, not the name. Relates ACTS-144/108/86/102 | 2026-09-04 |
| [ACTS-148](ACTS-148.md) | ACTS-144 | Oravia Marian design system + palette (Claude Design → styles.css tokens) | Done | — | **DONE 2026-09-04.** Design system (22 cards, claude.ai/design + `docs/brand/design-system/`), the **mark shipped** (cross-in-compass; replaced Lovable’s default favicon) and the live palette **harmonised** to it (16 tokens). Direction **Illuminated** locked; full system lives in `docs/brand/design-system/` + a Claude Design project. Tokens + component edits were applied and browser-verified, then **reverted at JC's call** ("launch with blue for now") — preserved as `stories/ACTS-148/illuminated-app.patch`. Dark split → ACTS-150. Split from ACTS-144 (palette track). Mock the **Marian palette** + component looks in the `/design` canvas first (deep Marian blue + warm ivory + restrained antique gold, burgundy accent), then translate to **central oklch tokens** in `src/styles.css`; verify light/dark + AA on pills/buttons/eyebrows/focus rings. Brand-mark art (continuous-thread "O" / Guiding Flame, favicon/theme_color) rides here. No `STORAGE_KEY` bump. Relates ACTS-144/90/96 | 2026-09-04 |
| [ACTS-151](ACTS-151.md) | ACTS-144 | Ship the Illuminated palette (warm parchment, gold bands, burgundy) | To Do | — | _New (JC) — split from ACTS-148 (2026-09-04)._ Direction **chosen and fully built**; JC chose to **launch the beta on blue**, so this is the decision to ship it. Preserved as `stories/ACTS-148/illuminated-app.patch` — ⚠️ predates the ACTS-148 harmonisation + header-mark commits, so **expect a rebase, not a clean apply**. Blues/golds already match; only **surfaces** change. Open first: burgundy has no home, Karla vs Mulish, whether to fold in ACTS-150. Relates ACTS-148/150/144 | 2026-09-04 |
| [ACTS-150](ACTS-150.md) | ACTS-144 | Wire dark mode (tokens exist, `.dark` never applied) | To Do | ACTS-148 | _New (JC) — split from ACTS-148 (2026-09-04, "dark ship later")._ `src/styles.css` has carried a full `.dark` token block for some time but **nothing in `src/` ever adds the `.dark` class** — it is unreachable dead code and the app renders light-only. Dark **values** are already chosen + contrast-verified in ACTS-148 (`docs/brand/design-system/`); this story is the **wiring**: a system/light/dark setting, no first-paint flash, `theme_color` following the active theme. No `STORAGE_KEY` bump expected. Relates ACTS-148/144 | 2026-09-04 |
| [ACTS-144](ACTS-144.md) | ACTS-144 | Rebrand name ACTS → Oravia | In Progress | — | **Name track — decisions locked** ([session-01](ACTS-144/session-01.md)). App name **Oravia** (*ora*+*via*, or-AH-vee-ah, not "OraVia"; no US trademark); **"MyOravia" = website/domain layer only, not the app wordmark** (`oravia.lovable.app` taken, `myoravia.com` erroring). Tagline **"Your Catholic life, connected."**; recurring idea **"Follow the sacred thread."** ACTS acronym **kept but repurposed** — retired as the app name, survives as a prayer feature (**follow-on ACTS-149**). **Palette split → ACTS-148.** **Sweep DONE + browser-verified** (session-01): `Brand.tsx` wordmark, beta gate, all route titles/meta, `about.tsx` copy, `FollowAlongView`, manifest name/short_name → **Oravia**; grep-clean of app-name "ACTS", `tsc` clean, **no `STORAGE_KEY` bump**. Tagline landed **"Your devotional life, gathered."** (retired "Catholic life, connected" as over-claiming AI/scope); wider brand voice (weaving/seek lines) captured for hero/onboarding, not yet placed. Pushed (`e109c05`, `2574a19`). Relates ACTS-86/143/90/101/148/149 | 2026-09-04 |
| [ACTS-145](ACTS-145.md) |  | Home Vessels pins — show status and link together (books, podcasts, programs) | To Do | — | _New (JC)._ **Scope: Vessels content pins on Home only** (`PinnedLinkRow`, `ownerType==="content"`, `hasStatus` book/program/video/podcast) — not Daily Rosary, not voice/reference. Today the pin shows a link-out but **never the status**. JC wants: **no link → status; has link → show both**. Reuse Formation `ContentRow`'s `hasStatus` pill. Relates ACTS-130/137/129 | 2026-09-03 |
| [ACTS-143](ACTS-143.md) |  | About the app — menu entry (vision, beta purpose, "everything is local") | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-143/session-01.md)). New `/about` route (AppShell) + **About** entry (Info icon) in `secondaryNavLinks` → side rail + mobile drawer. Copy from PRD v3.1, iterated with JC: epigraph → vision (become who God calls you to be; Scripture + tradition; discernment boundary) → "more than a prayer app" (faith learning → reflection → insights → wisdom) → "one place for your journey" (gather scattered paper/digital; hub that links out to Hallow/Bible-in-a-Year/catechism; journaling) → beta → local-only (link to Settings → Start over). Name stays "ACTS" until Oravia sweep. Commits `e29cf9c` (code), `346831f` (docs) — pushed. `tsc` clean. Relates ACTS-90/82/144 | 2026-09-04 |
| [ACTS-142](ACTS-142.md) |  | Themes for Lectio journaling (tag a sitting) + lexicon gaps | To Do | — | _Found closing ACTS-140._ Lectio journaling is never themed — `saveSessionReflection` sets no themes and the lexicon only *suggests* in free-write (never auto-assigns), so every sitting lands under "Untagged". Let a sitting be tagged from the Journal (ACTS-140 `SittingGroup`), suggest from its own text, and add "God's will"/obedience to the lexicon ("Love" already exists). Storage shape TBD. Relates ACTS-135/140/102/103 | 2026-09-03 |
| [ACTS-141](ACTS-141.md) |  | Don't leave empty Lectio sessions behind when Begin is abandoned | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-141/session-01.md)). Reap empty Lectio sessions (no linked reflections + no completed steps) two ways: a **load-time sweep** (`pruneEmptyLectioSessions`) and **on Close** (`leaveSession`). Chose prune over lazy-create; Lectio-scoped. Caught + fixed a self-inflicted regression — an unmount-effect prune deleted the just-opened session mid-navigation, so on-exit is the explicit Close (load sweep backstops back/swipe). No model change. Commit `0400f2d` (pushed). Relates ACTS-138/102/140 | 2026-09-03 |
| [ACTS-140](ACTS-140.md) |  | Group the journal by Lectio sitting (fold a session's movements together) | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-140/session-01.md)). A Lectio sitting folds into one collapsible unit across **Date + Theme + Source** — `buildJournalItems` folds movements (keyed by `prayer_session`, step-ordered), `groupJournalItems` buckets folded items so a sitting groups under one "Lectio Divina" source (fixes the dual-link double-count) / "Untagged" theme. `SittingGroup` shows date · passage · N movements + resume Open link; expanded shows pasted passage text at top then the movements. Empty sittings never appear. No model change. Commits `4f1e4e9`,`1a2fc5a` (pushed). Follow-up → themes-for-Lectio (unfiled). Relates ACTS-102/103/138/139/135 | 2026-09-03 |
| [ACTS-139](ACTS-139.md) |  | Reflections page — split into Write / Journal tabs | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-139/session-01.md)). `/reflections` split into **Write** (composer, default) / **Journal** (entries + group/sort) via the shared shadcn `Tabs` (matches Formation/Pray/Prayers); no new route. `?link=` lands on Write pre-linked; group/sort/expand state and the in-progress draft survive tab switches. Dropped the duplicative "Journal" heading. No `STORAGE_KEY` bump. Commits `005ce82`,`73049ac` + shared-Tabs restyle (in `4f1e4e9`) (pushed). Relates ACTS-103/138/140/135/136 | 2026-09-03 |
| [ACTS-138](ACTS-138.md) |  | Start a guided Lectio Divina from the Reflection surface | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-138/session-01.md)). "Reflect with Scripture" entry card (Flame icon) on Home + `/reflections` via the shared composer → one tap `startSession(LECTIO_TEMPLATE_ID)` into a fresh session. Lectio excluded from the free-write link picker (`buildReflectionLinkables`); card + "or write freely below" divider collapse once writing starts; copy: "Title or Subject", subtitle "Scripture Guided Writing or Inspired Free Writing". No new model, no `STORAGE_KEY` bump. Commits `752a4b8`,`b62164c`,`67d3817`,`08af614`,`721524e` (pushed). Follow-ups → ACTS-139/140/141. Relates ACTS-102/103/129 | 2026-09-03 |
| [ACTS-137](ACTS-137.md) |  | Item-level "Pin to Home" for Vessels (no favorited URL required) | In Progress | — | _Found finishing ACTS-136._ Home Vessels only surfaces **favorited links** (`pinnedLinks` + per-link `toggleContentLinkFavorite`); a `KnowledgeItem` has no item-level pin, so a book with no starred URL can't reach Home. Add `pinned?: boolean` (additive, no `STORAGE_KEY` bump) + item-level "Pin to Home" control + Home rendering of URL-less pins (`PinnedLinkRow` → internal `Link`). **High.** Relates ACTS-136/134/130 | 2026-09-03 |
| [ACTS-134](ACTS-134.md) |  | Vessels — A–Z sort within tiers + reseed USCCB/Hallow/Why We're Catholic | Done | — | ✅ Built + `tsc` clean. `byStatusThenTitle` (replaces `byStatusThenRecent`): status vessels first, then references, A–Z by title within each tier; removed `STATUS_RANK`; 4 call sites updated. STORAGE_KEY v38→v39 to resurface seeded USCCB/Hallow/*Why We're Catholic*. Pending commit | 2026-09-02 |
| [ACTS-133](ACTS-133.md) |  | Daily Rosary — launch an external app (Hallow) instead of the in-app session | Done | — | ✅ All ACs met. Setting-driven external launch (flat picker: Hallow/Amen/Come Pray the Rosary/Universalis/iBreviary/custom URL) with auto-log-as-prayed; verified universal links; `tsc` + browser clean. Shipped `6328770`,`ca32634`,`0a7807f`,`28022f3`. Final handoff `session-01.md` | 2026-09-02 |
| [ACTS-132](ACTS-132.md) |  | Session "Day N of M" counts from the session's own date | Done | — | ✅ Built + `tsc` clean. `session.$sessionId.tsx` counts "Day N of M" from `session.context.date`, not the plan's rolled-forward `date` (which advances each finish). Shipped `d89727a` | 2026-08-30 |
| [ACTS-131](ACTS-131.md) |  | Seed — Mater Dei Catholic Parish Prayer | Done | — | ✅ Built + browser-verified. `mater-dei-parish-prayer` (devotional) + body w/ Leader/All responsory + `src-mater-dei` source; STORAGE_KEY v37→v38. Pending commit | 2026-08-30 |
| [ACTS-130](ACTS-130.md) |  | Vessel status — only completable works; sort above references | Done | — | ✅ Built + browser-verified. `hasStatus()` (book/program/video/podcast); status pills gated + `byStatusThenRecent` ranks status-bearing first. "General website" = article/post (JC). Pending commit | 2026-08-30 |
| [ACTS-129](ACTS-129.md) |  | Reflection icon on session rows, session detail, and vessel items | Done | — | ✅ Built + browser-verified. Reflect NotebookPen on Home Continue/Today/Done rows, session-detail header, and Formation `ContentRow`; `/reflections?link=<id>` prefill (+ knowledge-detail button). Pending commit | 2026-08-30 |
| [ACTS-128](ACTS-128.md) |  | Seed — Novena: St. Michael (chaplet-based) | To Do | ACTS-122 | _New (ACTS-107 follow-on)._ Novena built on the **Chaplet of St. Michael** (`tpl-chaplet-michael`) in the swappable chaplet slot — the "rosary **or** chaplet" case. Blocked on ACTS-122's chaplet option. Gather public-domain wording | 2026-08-30 |
| [ACTS-127](ACTS-127.md) |  | Seed — Novena: Christmas | To Do | ACTS-123 | _New (ACTS-107 follow-on)._ St. Andrew repeated-prayer novena (15×/day) **or** day-by-day — confirm form w/ JC. Uses the day-sequenced pattern (ACTS-123). Gather wording | 2026-08-30 |
| [ACTS-126](ACTS-126.md) |  | Seed — Novena: Lenten | To Do | ACTS-123 | _New (ACTS-107 follow-on)._ Day-by-day Lenten prayers/meditations → depends on the "Novena Prayers" day-sequence pattern (ACTS-123). Pick the specific novena + gather wording | 2026-08-30 |
| [ACTS-125](ACTS-125.md) |  | Seed — Novena: 9-Day Mother of Perpetual Help | To Do | — | _New (ACTS-107 follow-on)._ Redemptorist Marian novena; reuses Loreto litany (`tpl-litany-loreto`); scaffold (ACTS-122) or prayer-sequence (ACTS-123) per source. Gather wording | 2026-08-30 |
| [ACTS-124](ACTS-124.md) |  | Seed — Novena: 9-Day Sacred Heart of Jesus | To Do | — | _New (ACTS-107 follow-on)._ Reuses the seeded Litany of the Sacred Heart (`tpl-litany-sacred-heart`); scaffold (ACTS-122) or prayer-sequence (ACTS-123) per source. Gather wording | 2026-08-30 |
| [ACTS-123](ACTS-123.md) |  | "Novena Prayers" pattern — day-sequenced prayers over N days | To Do | — | _New (ACTS-107 follow-on, enabler)._ One prayer/day over a period, or a different prayer each day (day-indexed, "Day N of M"). Unblocks Lenten/Christmas novenas. Decide day-index data shape | 2026-08-30 |
| [ACTS-122](ACTS-122.md) |  | Novena scaffold — chaplet slot + daily offerings | To Do | ACTS-107 | _New (ACTS-107 follow-on, enabler)._ Generalize the "Choose the parts" picker: add **chaplet** as a swappable option (rosary + litany **or** chaplet), generic novena-builder naming, day-varying/rotating offering prayers. Unblocks ACTS-128 | 2026-08-30 |
| [ACTS-121](ACTS-121.md) |  | Name + pronoun layer — dedicate a session "for whom" | In Progress | — | **Built + verified** (session-01). `substituteDedication` tokens `{name}{subj}{obj}{poss}{us}` in compiler; optional `SessionContext.for_whom`; builder "Prayed for" field; read-sites strip to generic; seeds retrofitted (Decade/Eternal Rest/FD/Loreto); STORAGE_KEY v32→v33. Harness 21/21; tsc clean; 0 token leaks. Ready for `/save`+`/done` | 2026-08-30 |
| [ACTS-120](ACTS-120.md) |  | Seed — Litany of the Faithful Departed + Loreto/BVM + closing collects | In Progress | — | **Seeded + verified** (session-01). `tpl-litany-faithful-departed` (52 salutations, plural refrains) + `tpl-litany-loreto` (50 Marian invocations, "pray for us") + `collect-fidelium`/`collect-departed-kindred` standalone prayers; sources; STORAGE_KEY v31→v32. Harness + tsc clean; both render, no console errors. Name/pronoun → ACTS-121. Ready for `/save`+`/done` | 2026-08-30 |
| [ACTS-119](ACTS-119.md) |  | Seed — Decade of the Passion (Rosary for the Dead) + How-To | In Progress | — | **Seeded + verified** (session-01). `tpl-rosary-for-the-dead` (rosary, Sorrowful-pinned): prep + "Merciful Jesus" large bead + 10 sufferings ×5 decades + Eternal Rest; source + how-to; STORAGE_KEY v30→v31. Harness 14/14; tsc clean; browser-verified. Name/pronoun deferred to ACTS-121. Ready for `/save`+`/done` | 2026-08-30 |
| [ACTS-118](ACTS-118.md) |  | Decide whether Vessels needs v8's Resource fields + external-app seeds | To Do | — | _New (from ACTS-117 reconcile; JC to decide, low pri)._ v8 §25D Resource Directory is marked "complete via Vessels" — decide: keep Vessels as-is, extend it with v8 resource fields (`app_store_url`, `access_model`, `best_for`…), or seed external apps (Hallow, Laudate…). Then drop the §25D open-question note. Relates ACTS-117/104/52. | 2026-08-29 |
| [ACTS-117](ACTS-117.md) |  | Rebase ACTS-PRD onto v8 + apply reconcile checklist | Done | — | ✅ All ACs met. `docs/ACTS-PRD.md` rebased onto v8 → v3 (restored 1A/9A/10A/12-seeds/23A-B/25A-E/31A-C/Wisdom/50-item DoD); structure + inventory + shipped-notes re-applied; `V8-CODE-GAP.md` decisions folded in; `ACTS-PRD-v3.docx` exported. Handoff [session-02](ACTS-117/session-02.md) | 2026-08-29 |
| [ACTS-116](ACTS-116.md) |  | ACTS status dashboard + signal-ingestion workflow | To Do | — | _Idea, grown from gap review._ Tiles + buckets + editable save-back doc, fronting a signal-ingestion loop (ingest → compare to built → classify built/partial/none/**suggested** → act). Path to **CRAVE** signal intake. Relates ACTS-104/105 | 2026-08-29 |
| ACTS-115 |  | HowTo versioning + multi-source | To Do | — | _v8 gap._ Add HowToVersion + HowToSource + USCCB Rosary How-To seed. Auto How-To exists (ACTS-28) | 2026-08-29 |
| ACTS-114 |  | Full Audio domain | To Do | — | _v8 gap, future/large._ Multi-speaker, voice-follow, record-during-session, usage tracking. Fields stubbed only | 2026-08-29 |
| ACTS-113 |  | Insights + Wisdom (future longitudinal intelligence) | To Do | — | _v8 gap, future._ Grounded Insights → user-named Wisdom w/ evidence links. Never AI-as-God's-will | 2026-08-29 |
| ACTS-112 |  | Prayer metadata: five Prayer Forms + Traditional/My origin | To Do | — | _v8 gap, low pri — validate need._ `prayer_form` + `prayer_origin_type` grouping | 2026-08-29 |
| ACTS-111 |  | Explore: My Intention vs Devotion Intention split | To Do | — | _v8 gap, low pri._ Today intention+petition kinds; explore a UserIntention/DevotionIntention split | 2026-08-29 |
| [ACTS-110](ACTS-110.md) |  | Nested Templates / Template Block | In Progress | — | **Code-complete** (session-01). `template_block` kind + recursive `expandTemplate` (per-block mystery resolution) + cycle guard + `MAX_BLOCK_DEPTH` + `source_template_id` lineage + builder "Devotion block" picker. Unit harness 14/14; tsc clean. Only open AC = downstream realization in **ACTS-107**. Ready for `/save`+`/done` | 2026-08-29 |
| [ACTS-136](ACTS-136.md) |  | Reflection composer — persist in-progress draft + specific daily-readings tag | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-136/session-01.md)). One shared localStorage draft (`reflectionDraft.ts`, kept out of `Database`) across both composers + Discard; daily-readings tag → liturgical day (Home + Reflect); shared `buildReflectionLinkables` picker (source parity + Mass group); in-progress affordance on Reflect. Folded in: Home Vessels status-first sort (ACTS-134 lineage had missed the pins path) + reflect icon on content pins. Item 4 resolved by the shared-draft/builder architecture. Commits `a1125cc`,`cdd5fd6`,`3a8d400` (pushed), `6c67519`,`535c05c` (pushed). Follow-up → ACTS-137 (item-level Home pin). Relates ACTS-103/129/102 | 2026-09-03 |
| [ACTS-135](ACTS-135.md) |  | Reflection organization — optional themes, no-AI tag suggestions, group-by view | Done | — | ✅ **Shipped + browser-verified** ([session-01](ACTS-135/session-01.md)). Optional `themes: string[]`; **no-AI** suggestion engine (`themes.ts`: ~34-theme lexicon + own tag-history, always-on Themes nudge, dismissible chips — 15/15 harness); `ThemeEditor` in composer + edit dialog; one-page **Group by: Date/Theme/Source** (collapsible, counts, catch-all) + asc/desc. Also **manual web link** (URL+label → new `link` target + `ReflectionLink.url`, Open↗ — 25/25 harness). AI semantic tagging = future opt-in (Cloud). Commits `5b70539`,`9fa7e41`,`7f48423` — pushed. Relates ACTS-103/102/136 | 2026-09-03 |
| [ACTS-134](ACTS-134.md) |  | Reflection capture by voice note + photo→OCR (media pipeline) | To Do | — | _Split from ACTS-103 (part B)._ Voice: record→transcribe→`body` (`mode:"spoken"`); Photo→OCR into editable `body`; both keep the attachment. Rides the deferred Cloud media phase (storage + transcription + OCR providers = external-contract decisions). Relates ACTS-103/102 | 2026-09-02 |
| [ACTS-109](ACTS-109.md) |  | Surface "Pray with the Pope" on Home as a daily session | To Do | — | _v8 gap._ Template **is** seeded (`tpl-pray-with-pope`); just surface on Home. Relates ACTS-10 | 2026-08-29 |
| [ACTS-108](ACTS-108.md) |  | Open Prayer — free-form "from the heart" component | Done | — | _v8 gap; JC wants._ ✅ **Shipped + verified** ([session-01](ACTS-108/session-01.md)): `open_prayer` kind (capture optional — an uncaptured prayer saves **empty + completed**, not pruned); ACTS header + "the app is not the prayer" helper text; seeded `tpl-open-prayer`; Prayers-tab "Pray now"; keep-as-Personal-Prayer with name prefill; never timed; self-finishing when it is the only step. **No app-side transcription** — the field takes keyboard dictation (JC, 2026-09-07). Also fixed a pre-existing share leak (ACTS-94). PRD v3.1. Relates ACTS-102/149/156 | 2026-09-07 |
| [ACTS-107](ACTS-107.md) |  | Novena — 9-Day Rosary for the Faithful Departed (swappable rosary/litany) | Done | ACTS-106 | ✅ **Shipped + verified** ([session-01](ACTS-107/session-01.md)). `tpl-litany-for-the-dead` → "Novena: 9-Day Rosary for the Faithful Departed" (daily×9, Pasiyam notes). Opening/Closing = loose editable prayers under headings; Rosary + Litany = **swappable blocks** via new `TemplateItem.block_options` + Session-Builder "Choose the parts" picker (choice rides in `plan.items`). New `offering-for-the-soul`; dedication now requires a name (no more "her" over a nameless soul); STORAGE_KEY v33→v37. Harness + browser verified. Follow-ons → ACTS-122–128. Pushed. | 2026-08-30 |
| [ACTS-106](ACTS-106.md) |  | Seed — Eternal Rest Prayer + surface "Why We're Catholic" (Trent Horn) on Home | Done | — | Shipped (`30aeddf`): Eternal Rest Prayer seeded (`src-eternal-rest`), book retitled "Why We're Catholic" + favorited → Home Vessels pin, STORAGE_KEY v29→v30. All ACs met + browser-verified. Final handoff [session-01](ACTS-106/session-01.md). Pushed. Feeds ACTS-107 | 2026-08-29 |
| [ACTS-102](ACTS-102.md) |  | Lectio Divina session — reflection as a first-class session step | Done | — | Shipped (`0192c89`): `reflection` step kind + inline capture → `Reflection` **dual-linked** to movement + session; 4-movement Lectio seed (titled instruction cards), empty-at-open per-session passage (Open-your-Bible + paste-propagation, reference-only), How To (5 links) + Unhurried Living attribution. STORAGE_KEY→v29. All ACs met + browser-verified. Final handoff [session-01](ACTS-102/session-01.md). Pushed. | 2026-08-29 |
| [ACTS-103](ACTS-103.md) |  | Reflection redesign — inspiration-in-view panel + voice note & OCR capture | In Progress | — | **Stub (vetted).** Reflection tab shows the inspiration below the box (resolve linked entity, or store an `excerpt` snapshot for pasted book passages); + voice note (transcribe → body, `mode:"spoken"`) and photo→OCR capture (Cloud media phase). Split candidate A/B. Relates ACTS-102 | 2026-08-29 |
| [ACTS-101](ACTS-101.md) |  | Rebrand "Faith Journey" → "ACTS" (acronym in header) | Done | — | Shipped (`efd316e`): ACTS wordmark + tagline (small letter-spaced caps, enlarged initials) across all headers + all titles/manifest. All ACs met. Final handoff [session-01](ACTS-101/session-01.md) | 2026-08-29 |
| [ACTS-100](ACTS-100.md) |  | Explore — daily rosary "choose session" (point daily at an already-scheduled session) | To Do | ACTS-99 | **Exploration/parked.** Add a "choose session" option beside "switch template" → point the Daily Rosary at an existing scheduled session (countdown surfacing) + model petition/thanksgiving phases (27+27). Current behavior fine as-is | 2026-08-29 |
| [ACTS-99](ACTS-99.md) |  | Daily rosary defers to a scheduled novena rosary | Done | — | Shipped (`209a5f8`): defer toggle in builder (rosary+bounded), pinned DAILY ROSARY row, "Day X of N" (start-date-aware), overlap warn/block, Home mirrors it. All 8 ACs met + browser-verified. Final handoff [session-01](ACTS-99/session-01.md). Follow-up: ACTS-100 | 2026-08-29 |
| [ACTS-98](ACTS-98.md) |  | Month calendar in Plan > Sessions — color-coded day dots + upcoming list | To Do | — | **Low priority.** Month grid on the Sessions view (no new tab): per-day dots for sessions/planned/readings/programs/reflections + upcoming-next-month list; supersede orphaned `/calendar`. Confirm dot-color encoding w/ JC | 2026-08-28 |
| [ACTS-97](ACTS-97.md) |  | Land on the browse/list tab by default (Vessels + Plan), not the create tab | Done | — | Shipped (`fda1934`): Vessels→Library, Plan→Sessions + `?build` for New session. All ACs met (JC spot-checked). Final handoff [session-01](ACTS-97/session-01.md) | 2026-08-28 |
| [ACTS-96](ACTS-96.md) |  | Make the app mobile-web-first and responsive (wide-screen nav + PWA) | Done | — | Shipped: responsive shell (side rail md+, mobile menu drawer, removed More), Plan=calendar icon, + PWA (manifest/icons/offline SW, hand-rolled). All 6 ACs met; un-parks ACTS-90 | 2026-08-28 |
| [ACTS-95](ACTS-95.md) |  | Pray a shared session in the app — adopt a `/follow` link into your sessions (+ sign in to save) | To Do | ACTS-94 | Add "Pray in the app" on `/follow`; adopt payload → stored session → Prayer Mode; sign-in saves to sessions list | 2026-08-28 |
| [ACTS-94](ACTS-94.md) |  | Guest "follow-along" share — read-only view + short titled backend links | Done | — | Shipped: `/follow` + `/follow/<slug>`, share dialog+QR, upcoming-row share, Supabase `shared_sessions`. App-user adopt → ACTS-95 | 2026-08-28 |
| [ACTS-93](ACTS-93.md) |  | Explore — share read-only "follow-along" prayer view for guests (no app) | Done | — | Spike done: fragment-link approach validated (rosary ~3 KB); impl → ACTS-94 | 2026-08-27 |
| [ACTS-92](ACTS-92.md) |  | Set up the test harness (Vitest + Testing Library + Playwright) — deferred | To Do | — | Install Vitest/Playwright; smoke test per layer; backfill ACTS-76 tests | 2026-08-27 |
| [ACTS-91](ACTS-91.md) |  | Testing convention — tests documented + tracked as a task | Done | — | Convention landed; harness build → ACTS-92 | 2026-08-27 |
| [ACTS-76](ACTS-76.md) |  | Pray-mode tracker — current item, grayed-out completed, auto-scroll | Done | — | Shipped + pushed; tests backfilled under ACTS-92 | 2026-08-27 |
| [ACTS-75](ACTS-75.md) |  | Number the backlog into ACTS stories + EPIC column + process docs | Done | — | Follow-ups (not blockers): push `main`; JC fills EPIC values | 2026-08-25 |
| ACTS-01…ACTS-74 |  | _Historical Done work_ | Done | — | See the [full ledger](../docs/JIRA-BACKLOG.md) | 2026-08-25 |

_Statuses: **To Do** · **In Progress** · **Blocked** · **Done**._

**Testing convention (ACTS-91, Done):** every code-change story **documents** its tests in a
**Tests** section (unit · integration · E2E) — enforced by [`_TEMPLATE.md`](_TEMPLATE.md) — and
its testing work is **tracked as a task**. The shared E2E flow catalog is
[`docs/E2E-TEST-PLAN.md`](../docs/E2E-TEST-PLAN.md). No runner is wired yet — the harness build
is deferred to **ACTS-92**, so coverage stays **planned** until then.

## How the backlog is numbered

- Every completed unit of work is one `ACTS-NN` row, numbered **oldest-first by commit**.
- Numbers are **permanent** — never renumbered; new work takes the next number.
- Each row **logs its commit(s)**; the **EPIC** column is left blank for JC to fill in.
- [`.counter`](.counter) holds the last-used number (**97**); brand-new work = ACTS-98.
- Full detail + the maintenance process: [`docs/JIRA-BACKLOG.md` → Process](../docs/JIRA-BACKLOG.md#process).

## Open (numbered — ready to `/start`)

Every open story now has an id so you can reference it when starting a chat — see the
[Open section of the ledger](../docs/JIRA-BACKLOG.md#-open--backlog-numbered), **ACTS-76…90**.
Highlights / recently added:

- **ACTS-104** — Vision PRD v2: reconcile doc with reality + the global `/prd-sync` skill; PRD now in-repo at [`docs/ACTS-PRD.md`](../docs/ACTS-PRD.md) → [`ACTS-104.md`](ACTS-104.md) _(Done)_.
- **ACTS-105** — Evolve `/prd-sync` → a "canon / doc sync" (vision change cascades to related docs) + apply to CRV → [`ACTS-105.md`](ACTS-105.md) _(To Do)_.
- **ACTS-92** — Set up the test harness (Vitest + Testing Library + Playwright), **deferred** → [`ACTS-92.md`](ACTS-92.md). **Blocks executable tests for every story.**
- **ACTS-91** — Testing convention (tests documented + tracked as a task) → [`ACTS-91.md`](ACTS-91.md) _(Done)_.
- **ACTS-76** — Pray-mode tracker: prominent current item, grayed-out completed, auto-scroll → [`ACTS-76.md`](ACTS-76.md) _(Done)_.
- **ACTS-78** — Push `main` + Publish in Lovable (merge already done locally; just push + Publish).
- **ACTS-82** — Enable Supabase persistence (backend for auth; parked as a future story).
- **ACTS-87 / ACTS-88** — Auth (email login + session) / Account creation (sign up with email).
- **ACTS-89** — Guided-prayer expand/collapse + expand-all/collapse-all → [`ACTS-89.md`](ACTS-89.md) _(Done)_.
- **ACTS-90** — Platform: mobile-first, mobile web, no app store — **recorded decision** (PWA leaning but parked) → [`ACTS-90.md`](ACTS-90.md).

To begin one in a clean chat: `/start ACTS-NN`.
