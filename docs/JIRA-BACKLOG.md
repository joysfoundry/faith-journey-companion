# ACTS backlog — faith-journey-companion

The numbered story ledger for this repo. **One story per row**, numbered `ACTS-NN`
oldest-first by commit, with the commit(s) that delivered it logged in the last column.
The **EPIC** column is intentionally left blank for JC to fill in — group rows into
epics however you like (a suggested grouping is noted in the section headers).

- **IDs:** `ACTS-NN`, zero-padded, drawn from [`stories/.counter`](../stories/.counter)
  (holds the last-used number; next id = counter+1). This chat is the latest, **ACTS-75**.
- **Board (active view):** [`stories/README.md`](../stories/README.md) — the compact,
  newest-on-top board. This file is the full historical ledger behind it.
- **How this is maintained:** see [Process](#process) at the bottom.

_Statuses: **To Do** · **In Progress** · **Blocked** · **Done**. Commits are on branch
`prd-gap-merge` unless noted._

---

## Foundation — initial build & PRD implementation (Aug 16–17)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-01 |  | Task  | Scaffold app from TanStack Start template | Done | `3190f79` |
| ACTS-02 |  | Task  | Create PRD implementation plan | Done | `f996d12` |
| ACTS-03 |  | Story | Photo gallery MVP | Done | `3aa9218` |
| ACTS-04 |  | Story | Rebuild landing page & nav | Done | `995eece` |
| ACTS-05 |  | Story | Restructure Home → Prayer | Done | `c362d36` |
| ACTS-06 |  | Story | Add reflections to library | Done | `8041a9d` |
| ACTS-07 |  | Task  | Wire app & rename Library | Done | `3d84f42` |
| ACTS-08 |  | Story | Add Reflect menu & Word section | Done | `b6c64eb`, `6b93c07` |

---

## PRD gap-merge (Aug 18–19) — suggested epic: "ACTS PRD gap-merge into Faith Journey"

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-09 |  | Task  | Merge Add Prayers into a tabbed route | Done | `ec2f379` |
| ACTS-10 |  | Story | Generic External Link component + "Pray with the Pope" preset | Done | `db2af68` |
| ACTS-11 |  | Story | Seed Chaplet of St. Michael | Done | `db2af68` |
| ACTS-12 |  | Story | Generic Scripture component (author-placeable, has citation) | Done | `e0c36fc` |
| ACTS-13 |  | Story | Seed Scriptural Rosary (Luminous) — Scripture before each Hail Mary | Done | `e0c36fc`, `8551e9d` |
| ACTS-14 |  | Story | Promote Reflection / Learning / Mass to persisted entities | Done | `bc3009d` |
| ACTS-15 |  | Task  | Rename "Formation" → "Learn"; collection = "Life Library" | Done | `ba8dcb3` |
| ACTS-16 |  | Task  | Remove home greeting (user replacing it) | Done | `0e7c4b6` |
| ACTS-17 |  | Story | Redesign "Add Prayer" — single vs devotion; manual / URL / photo intake | Done | `8bd66c3` |
| ACTS-18 |  | Story | PrayerMedia — audio/video links + short recorded/uploaded clips (≤1.5 MB) | Done | `8bd66c3` |
| ACTS-19 |  | Story | Add-Prayer review-before-save + URL fetch returns page title | Done | `8bd66c3` |
| ACTS-20 |  | Story | Prayer library — read-only details view + row actions (pray / edit / expand) | Done | `db345d2` |
| ACTS-21 |  | Bug   | Fix prayer editor hydration race on hard reload / deep-link | Done | `4ce5fb1` |
| ACTS-22 |  | Story | Redesign devotion builder — add+type, review, auto How-To | Done | `e79af8f` |
| ACTS-23 |  | Story | Builder: inline insert-between + fully-expanded Preview | Done | `fe11b22` |
| ACTS-24 |  | Story | Builder: hover-only "+" type dropdown; Source name + URL | Done | `1f0fba1` |
| ACTS-25 |  | Story | Fixed mystery set on templates (pin Luminous, etc.) | Done | `e79af8f` |
| ACTS-26 |  | Story | Petition + Meditation item kinds | Done | `e79af8f` |
| ACTS-27 |  | Story | Template-level audio links (uploads stubbed) | Done | `e79af8f` |
| ACTS-28 |  | Story | Auto-generate a numbered "How to pray …" guide on save | Done | `e79af8f` |
| ACTS-29 |  | Bug   | Mysteries show "First Luminous Mystery" + title + description everywhere | Done | `8551e9d` |
| ACTS-30 |  | Story | Rosary: label each decade ("1st decade" …) in Pray mode | Done | `f0ef646`, docs `7a419e3` |

---

## Prayer Sessions / Session Builder (Aug 20)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-31 |  | Story | Session Builder: saved sessions, listen picker + scheduler fields | Done | `6ca5f42` |
| ACTS-32 |  | Task  | Extract shared DevotionItemsEditor; reuse in Template Builder | Done | `36c8ed2` |
| ACTS-33 |  | Story | Session Builder: editable items, optional template, save-as-template | Done | `13e3a7f` |
| ACTS-34 |  | Story | Session Builder ⋯ menu — rename, delete, clear, save | Done | `6ed65c9`, `f855431`, `dbc7640` |
| ACTS-35 |  | Story | Builder items: collapse by default, caret to expand | Done | `900bfb8` |
| ACTS-36 |  | Story | Session Builder: custom recurrence, Liturgy Hours, duration | Done | `9009194` |
| ACTS-37 |  | Story | Prayer Sessions: split into Session Builder + Sessions tabs | Done | `a03c333` |
| ACTS-38 |  | Task  | Naming: reconcile "template" → "devotion" across the UI | Done | `3fa2d45` |
| ACTS-39 |  | Story | Sessions: duplicate action + recurrence rolls forward on finish | Done | `6e2764f` |
| ACTS-40 |  | Story | Landing search: pray a prayer, and finishing saves it as a session | Done | `a6a4109`, docs `894d0ac`, `af79346` |

---

## Recurrence & library redesign (Aug 21)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-41 |  | Story | Unified calendar recurrence (RRULE), novena removal, library redesign | Done | `528394f`, docs `8d84f37` |

---

## USCCB basic prayers seed (Aug 21)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-42 |  | Story | Seed USCCB basic prayers; point USCCB source at basic-prayers; STORAGE_KEY v7→v8 | Done | `e2d834a`, `36a8596`, docs `f997ed4`/`749babe`/`dcb9afb`/`713c5b3` |

---

## Song / hymn prayer type (Aug 21)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-43 |  | Story | Add Song prayer type with selectable verse/chorus segments | Done | `0684468` |
| ACTS-44 |  | Story | Seed Caro hymns + family prayers; rebuild Caro Family Rosary | Done | `f00ec43`, docs `2caa2cc` |

---

## Home redesign & session states (Aug 21–22)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-45 |  | Story | Home redesign: section cards, editable daily devotion, today's sessions | Done | `0b4bff4` |
| ACTS-46 |  | Story | Reflection journal: single-entry view with edit/delete | Done | `0531613` |
| ACTS-47 |  | Story | Home: show completed sessions as Done (start / continue / done) | Done | `c614efc`, `2fffb85` |
| ACTS-48 |  | Bug   | Sessions: name a single ad-hoc prayer by its own name, not "Session" | Done | `4b3b58a` |
| ACTS-49 |  | Task  | Prayer picker: sort prayers/songs alphabetically by title | Done | `4ad7b57` |
| ACTS-50 |  | Task  | Nav: rename the Pray tab to Plan (route unchanged) | Done | `330e2ae` |
| ACTS-51 |  | Task  | Home: typography cohesion across the cards | Done | `0868d1c` |

---

## Knowledge library — unify (Aug 22)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-52 |  | Story | Knowledge library: unify Learn, Programs, and Resources into one model | Done | `3ee7760` |
| ACTS-53 |  | Story | Seed "Why We Are Catholic" (Trent Horn); bump STORAGE_KEY v14 | Done | `31095f1` |

---

## Liturgical day titles (Aug 22)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-54 |  | Story | Home Word card: name the liturgical day (season + saint/feast/solemnity) | Done | `86669ab`, TODO `dc1fc15` |

---

## Mystery-detail versions (Aug 22)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-55 |  | Story | Mysteries: selectable body versions per mystery | Done | `68d9627` |
| ACTS-56 |  | Story | Mysteries: version authoring editor | Done | `fe1b7e9`, docs `33661af` |

---

## Litany devotions (Aug 22)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-57 |  | Story | Litany: seed three public-domain litanies as devotions | Done | `7570737`, docs `2b2f7a8` |

---

## Knowledge → Vessels (Voice / Channel / Content) (Aug 23–24)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-58 |  | Bug   | Fix: compute today's date in local time, not UTC | Done | `01b1ad8` |
| ACTS-59 |  | Story | Knowledge: Voice → Channel → Content model | Done | `fc3dc0c` |
| ACTS-60 |  | Story | Knowledge: seed author Voices; attribute books to them | Done | `ba2e444` |
| ACTS-61 |  | Story | Knowledge: unify add and edit onto one record page | Done | `2df7588`, `effc1c3` |
| ACTS-62 |  | Story | Word: show the author Voice in the reading-program subtitle | Done | `aa61f04`, `ce488e4`, docs `ca3ed61` |
| ACTS-63 |  | Story | Knowledge: surface content channel_id in the UI | Done | `6da5038` |
| ACTS-64 |  | Story | Knowledge: inline "By Voice" grouped Library view (+ expand/collapse) | Done | `7c34253`, `b540884`, `ee12285` |
| ACTS-65 |  | Task  | Knowledge: clearer content count on Voice rows | Done | `f51948d` |
| ACTS-66 |  | Bug   | Knowledge: stop empty draft Voices becoming "Untitled" ghosts | Done | `ae7024a` |
| ACTS-67 |  | Story | Knowledge: byline falls back to link platform for unattributed content | Done | `db17f3a`, docs `7c4f105` |
| ACTS-68 |  | Story | Knowledge: add Quote as a content type | Done | `6ec2742` |
| ACTS-69 |  | Story | Seed: St. Padre Pio Voice + a quote as the Quote-type example | Done | `e851154` |
| ACTS-70 |  | Story | Knowledge: add search to the Library, like the Prayers page | Done | `8c98d2f` |
| ACTS-71 |  | Task  | Rename: Voices → Vessels, and the section Knowledge → Vessels | Done | `6e607f2`, docs `2afc219` |

---

## Account Settings + Bible app (Aug 25)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-72 |  | Story | Links: open all external links in a new tab (ExternalLink component) | Done | `9f94632`, `5a8b753` |
| ACTS-73 |  | Story | Settings: add Bible app, translation, and Online Bible link | Done | `1527cdd`, docs `b476fd0` |

---

## Workflow & tracking (Aug 25)

| # | EPIC | Type | Summary | Status | Commit(s) |
|---|------|------|---------|--------|-----------|
| ACTS-74 |  | Chore | Set up local ACTS story board (Git-tracked, no Jira) | Done | `b1d7e74` |
| [ACTS-75](../stories/ACTS-75.md) |  | Chore | Number the backlog into ACTS stories (oldest-first) + EPIC column + process docs | Done | `f33c564` … `2ab5d5f` (this chat) |

---

## 🔜 Open — backlog (numbered)

Every open story now carries an `ACTS-NN` id so you can reference it when starting a chat
(`/start ACTS-NN`). Not yet started — IDs are reserved; `.counter` is **157**, so brand-new
work starts at ACTS-158. **An id links to its pointer file when one exists**
(created when the story is started); otherwise it's plain text.

| # | EPIC | Priority | Type | Status | Summary | Detail |
|---|---|---|---|---|---|---|
| [ACTS-76](../stories/ACTS-76.md) |  | P1 | Story | **Done** | **Pray-mode tracker (ACTS-style)** | Prominent current item (NOW badge + ring), **completed prayers grayed out**, **auto-scroll** as you advance + on tab-switch. Shipped `ee726b3`/`61cf1ca`; tests planned under ACTS-92. |
| ACTS-77 |  | P2 | Story | To Do | **Touch drag-and-drop in builder** | Current reorder uses HTML5 DnD (desktop only). Add a touch lib (e.g. dnd-kit). |
| ACTS-78 |  | P2 | Task | To Do | **Push `main` + Publish in Lovable** | Merge → `main` already done locally (fast-forward to `f9f51a0`); PR not needed. Remaining: `git push origin main`, then confirm Lovable sync + click **Publish** for the public `*.lovable.app` URL. |
| ACTS-79 |  | P2 | Story | To Do | **Mystery-detail variant picker (Scripture vs meditation)** | Partly delivered by ACTS-55/56 (selectable bodies + authoring editor). Remaining: let the Session/Template **choose which variant to present**. Examples: `~/Downloads/Mysteries 1.md` (Scripture), `Mysteries 2.md` (meditation). |
| ACTS-80 |  | P3 | Story | To Do | **Per-prayer media in devotion import** | Audio/video attaches to single prayers; bundle import currently doesn't — add media per detected prayer. |
| ACTS-81 |  | P3 | Story | To Do | **Real OCR for photo intake** | Photo intake is a manual-transcribe seam; wire client-side OCR (tesseract.js) or cloud. |
| ACTS-82 |  | P3 | Story | To Do | **Enable Supabase persistence** | fjc's snake_case model maps 1:1 to Postgres; move the localStorage store to Supabase. Unlocks real shared/persistent data for a public audience — and is the **backend for auth** (ACTS-87/88). |
| ACTS-83 |  | P3 | Bug | To Do | **Hydration race on remaining edit routes** | Fixed for prayer editor + template builder; audit other `useState`-from-store routes and gate on `ready`. |
| ACTS-84 |  | P3 | Task | To Do | **Repetition-count input design** | Current ×N control is a −/+ stepper — tedious for ×10/×53. Recommend tap-the-"×N"-to-type. |
| ACTS-85 |  | P4 | Story | To Do | **Restore "start from existing template" (Duplicate)** | Removed in the builder redesign; re-add as a Duplicate action if wanted. |
| ACTS-86 |  | P4 | Story | To Do | **ACTS framing** | Thread Adoration · Contrition · Thanksgiving · Supplication where it fits (branding / prayer categorization). |
| ACTS-87 |  | P2 | Story | To Do | **Auth — email login + session** | _New._ Email/password sign-in, session persistence, sign-out, protected/authed state in the app shell. Likely **Supabase Auth** → depends on / relates to ACTS-82. |
| ACTS-88 |  | P2 | Story | To Do | **Account creation — sign up with email** | _New._ Email sign-up + verification, create a profile/account record, then the localStorage data becomes per-account. Pairs with ACTS-87; relates to ACTS-82. |
| [ACTS-89](../stories/ACTS-89.md) |  | P2 | Story | To Do | **Guided-prayer expand/collapse + expand-all/collapse-all** | _New (spinoff)._ Per-prayer expand/collapse **and** an expand-all/collapse-all control in the **guided prayer (Pray mode)** view (`src/routes/pray.tsx`); test the usage/interaction in that view. |
| [ACTS-90](../stories/ACTS-90.md) |  | — | Decision | Decided | **Platform — mobile-first, mobile web, no app store** | _Recorded decision (not a build story)._ Mobile-first, delivered as **mobile web (web-view first), NOT the app store**. **Leaning PWA but parked** — don't build PWA plumbing until JC confirms. |
| [ACTS-91](../stories/ACTS-91.md) |  | P2 | Story | **Done** | **Testing convention — tests documented + tracked as a task** | Every code-change story documents tests (Tests section in [`_TEMPLATE.md`](../stories/_TEMPLATE.md)) and tracks the testing work; shared E2E flow catalog [`docs/E2E-TEST-PLAN.md`](E2E-TEST-PLAN.md); Process rule #7. Harness build split to ACTS-92. |
| [ACTS-92](../stories/ACTS-92.md) |  | P2 | Story | To Do | **Set up the test harness (deferred)** | _Spun off from ACTS-91._ Vitest + Testing Library (unit/integration) + Playwright (E2E); mock `scrollIntoView`/`matchMedia`/`localStorage`; `test` + `test:e2e` scripts; smoke test per layer; CI on push; backfill ACTS-76 tests. **Blocks executable tests for every story.** |
| [ACTS-93](../stories/ACTS-93.md) |  | P3 | Spike | **Done** | **Explore — share a read-only "follow-along" prayer view for guests (no app)** | Spike: can someone with no app follow along with a session you are praying? Validated the **fragment-link** approach — the whole session encodes into the URL fragment (a rosary lands at **~3 KB**), so a guest view needs no backend and no account. Payload probe + findings recorded in the pointer. Implementation → **ACTS-94**. Commit `0d4bc5d`. |
| [ACTS-94](../stories/ACTS-94.md) |  | P2 | Story | **Done** | **Guest "follow-along" share — read-only view + short titled backend links** | Built in phases: a **share codec** (`lz-string`) for the fragment payload, `ItemView` extracted into a shared prayer component so guest and app render identically, the read-only **`/follow`** route, a **Share dialog** with a link + best-effort QR and entry points (incl. the upcoming-session row), then **short titled links** `/follow/<slug>` backed by a new Supabase **`shared_sessions`** table with public RLS (`src/lib/prayer/shareStore.ts`). Guests bypass the beta gate entirely. Commits `4ecba46`, `d22c800`, `7991b4e`, `c1fd532`, `fcf6a94`, `7dd1b81`, `003e28f`, `4ea6aff`, `d4516e1`, `8450596`. App-user adopt → **ACTS-95**. |
| [ACTS-95](../stories/ACTS-95.md) |  | P3 | Story | To Do | **Pray a shared session in the app — adopt a `/follow` link into your sessions** | _Parked (`b0a1533`)._ The guest view (**ACTS-94**) is read-only. Add **"Pray in the app"** on `/follow`: adopt the decoded payload into a stored session and open **Prayer Mode**, so a shared session becomes yours — with sign-in saving it to your sessions list. Depends **ACTS-94**; relates ACTS-82/87/88 (accounts). |
| [ACTS-96](../stories/ACTS-96.md) |  | P1 | Story | **Done** | **Make the app mobile-web-first and responsive (wide-screen nav + PWA)** | Two parts. **Responsive shell** — a side rail from `md` up, a mobile menu drawer, and the "More" page removed (its entries folded into the drawer); Plan took the calendar icon. **PWA** — hand-rolled, no deps: `manifest.webmanifest`, icons, and an offline service worker (`public/sw.js`, the same file whose `VERSION` later had to be bumped for the ACTS-148 icon swap). All 6 ACs met; **un-parked ACTS-90**. Commits `7d0f041`, `e68939b`, `c599bb8`, `959aa56`. |
| [ACTS-97](../stories/ACTS-97.md) |  | P3 | Task | **Done** | **Land on the browse/list tab by default (Vessels + Plan), not the create tab** | Opening a section dropped you on its *create* form. Now **Vessels → Library** and **Plan → Sessions**, with `?build` as the explicit route to "New session". All ACs met (JC spot-checked). Commits `3cefcb9`, `fda1934`, `9208dd1`, `b0e6d70`, `ec2a767`. Handoff [session-01](../stories/ACTS-97/session-01.md). |
| [ACTS-98](../stories/ACTS-98.md) |  | P4 | Story | To Do | **Month calendar in Plan > Sessions — color-coded dots + upcoming list** | _New._ **Low priority.** Month grid on the **Sessions** view (no new tab/route): each day shows color-coded dots for prayer sessions / planned sessions / daily readings / programs / past reflections; below it, a list of upcoming sessions for the next month. Supersede the orphaned `/calendar` route. Dot-color encoding TBD w/ JC. Relates ACTS-96/97. |
| [ACTS-99](../stories/ACTS-99.md) |  | P2 | Story | **Done** | **Daily rosary defers to a scheduled novena rosary** | The countdown path: a **defer toggle** in the Session Builder (available for a rosary with bounded recurrence) sets `fulfills_daily_rosary`, so the pinned **DAILY ROSARY** row runs that novena and reads **"Day X of N"** (start-date-aware), reverting to the standard daily when the novena finishes; overlapping plans warn/block, and Home mirrors the same row. All 8 ACs met + browser-verified. Commits `160b736`, `209a5f8`, `14b2c28`. Handoff [session-01](../stories/ACTS-99/session-01.md). Follow-ups → **ACTS-100**, and the external-app path **ACTS-133**. |
| [ACTS-100](../stories/ACTS-100.md) |  | P4 | Spike | To Do | **Explore — daily rosary "choose session" (point the daily at a scheduled session)** | _Filed via `/spinoff` (`92cd265`); parked by JC 2026-08-29 — "the current template-switch behavior is fine as-is."_ Two separable ideas. **(A) A "choose session" entry point** on the Daily Rosary control (Home card + the pinned DAILY ROSARY row) pointing the daily at an **already-scheduled session** — i.e. surfacing ACTS-99's `fulfills_daily_rosary` as a picker, not only a builder toggle. **(B) Petition / thanksgiving phases**, genuinely unmodelled today: nothing expresses the 54-day novena's **27 + 27** split or a day-index-driven mystery sequence — weigh a general `phases: [{label, days}]` ("Petition · Day 12 of 27") against hardcoding `tpl-54-novena`. Note the deliberate contrast: **switch-template = endless daily prayers, no countdown, by design**. Depends **ACTS-99**; relates ACTS-98. |
| [ACTS-101](../stories/ACTS-101.md) |  | P2 | Story | **Done** | **Rebrand "Faith Journey" → "ACTS" (acronym in the header)** | The **first** rebrand — ACTS wordmark + tagline as small letter-spaced caps with enlarged initials (a script face was tried and dropped), applied across every header, route title and the manifest. All ACs met. Commits `29cc20f`, `8357c68`, `efd316e`, `77970cb`. Handoff [session-01](../stories/ACTS-101/session-01.md). Superseded by the **Oravia** rebrand (**ACTS-144**), which kept ACTS as a prayer feature (**ACTS-149**). |
| [ACTS-102](../stories/ACTS-102.md) |  | P3 | Story | **Done** | **Lectio Divina session — reflection as a first-class session step** | Shipped (`0192c89`). 4-movement Lectio (Read/Reflect/Respond/Rest) = session + prayer + reflection. `reflection` step kind (`TemplateItemKind`+`SessionItemKind`) + inline capture → `Reflection` (`mode:"written"`) **dual-linked** to movement (`session_item`) + session (`prayer_session`); one `db.reflections` store. Titled instruction cards; empty-at-open per-session passage (reference-only + Open-in-Bible + paste-propagation); How To with 5 links + Unhurried Living (Fadling) attribution. STORAGE_KEY→v29. All ACs met + browser-verified; handoff `stories/ACTS-102/session-01.md`. Relates ACTS-103. |
| [ACTS-103](../stories/ACTS-103.md) |  | P4 | Story | To Do | **Reflection redesign — inspiration-in-view panel + voice note & OCR capture** | _New (stub, vetted)._ Reflection tab shows the inspiration in plain sight (resolve linked entity, or store an `excerpt` snapshot on `ReflectionLink` for pasted book passages) + capture by **voice note** (transcribe → `body`, `mode:"spoken"`) and **photo→OCR** of a paper journal (Cloud media phase; `photo_count` reserved). Split candidate: (A) view panel vs. (B) voice+OCR. Relates ACTS-102. |
| [ACTS-104](../stories/ACTS-104.md) |  | P2 | Story | **Done** | **Vision PRD v2 — reconcile doc with reality + /prd-sync skill** | Moved the combined business+technical PRD in-repo as [`docs/ACTS-PRD.md`](ACTS-PRD.md) (v2): Faith Journey=umbrella / **ACTS**=prayer-first product; added the missing front matter (What it is · Problem · Solution/wedge · Origin · Position) + a `[Shipped]/[Partial]/[Future]` "What's shipped today" inventory; taxonomy + shipped-notes updated; new §35 Share & Follow-Along. Built the **global** `/prd-sync` skill (`~/.claude/skills/prd-sync`, not repo-tracked) + `docs/.prd-sync` pointer + `.docx` for Google. Relates ACTS-101/102/105. |
| [ACTS-105](../stories/ACTS-105.md) |  | P3 | Story | To Do | **Evolve /prd-sync → "canon / doc sync" + apply to CRV** | _New._ Grow `/prd-sync` from single-PRD reconcile into a **canon/doc sync**: change the canonical source (e.g. the vision) → cascade proposed updates to all related/derived docs (non-destructive, review-first). Generalize `docs/.prd-sync` into a canon+derived manifest; protect hand-edited docs; validate on **CRV** too. Depends ACTS-104. |
| [ACTS-106](../stories/ACTS-106.md) |  | P2 | Task | **Done** | **Seed — Eternal Rest Prayer + surface "Why We're Catholic" (Trent Horn) on Home** | Filed via `/spinoff` from the ACTS-104/PRD-v2 fork (`0cd3a4d`). Seeded the **Eternal Rest Prayer** (`eternal-rest` + `src-eternal-rest`; devotional · vocal · traditional, source recorded with `known` provenance) — the component the novena (**ACTS-107**) then reuses. Also **corrected the Trent Horn book title** from the seeded "Why We Are Catholic" to the real **"Why We're Catholic"**, and surfaced it on Home via `favorite: true` on its link → the Home Vessels pin (reading status deliberately left `not_started`; the pin comes from the favorited link, not from status). `STORAGE_KEY` **v29→v30** for the reseed. `tsc` clean + browser-verified. Commits `30aeddf`, `c000d8b`, `01c3765`. Handoff [session-01](../stories/ACTS-106/session-01.md). Relates ACTS-107/53. |
| [ACTS-107](../stories/ACTS-107.md) |  | P1 | Story | **Done** | **Novena — 9-Day Rosary for the Faithful Departed (swappable rosary/litany blocks)** | ✅ Shipped + verified ([session-01](../stories/ACTS-107/session-01.md)). `tpl-litany-for-the-dead` → **"Novena: 9-Day Rosary for the Faithful Departed"** (daily × 9, with Pasiyam notes). Opening/Closing are loose editable prayers under headings; the **Rosary and Litany are swappable blocks** — a new `TemplateItem.block_options` plus a Session-Builder **"Choose the parts"** picker, with the choice riding in `plan.items`. That scaffold is the reusable piece the rest of the novena roadmap builds on. New `offering-for-the-soul`; dedication now **requires a name** (no more praying "her" over a nameless soul). `STORAGE_KEY` **v33→v37**. Harness + browser verified. Commits `482bf75`, `b52885b`, `ff61599`, `bf70c8e`, `3c48557` (novena domain reference in PRD §6). Depends **ACTS-106**; follow-ons **ACTS-122–128**. |
| [ACTS-108](../stories/ACTS-108.md) |  | P2 | Story | To Do | **Open Prayer — free-form "from the heart" component** | _New (v8 gap review; JC wants)._ PRD v8 §23A/23B. No `open_prayer` item kind today. Words *to God* (distinct from intention/petition and from Reflection). Capture: write / speak→transcribe / leave-open / pray-without-capture; optional save-as-Personal-Prayer; transcription toggle distinct from Voice-Follow & Record. Relates ACTS-102. |
| [ACTS-109](../stories/ACTS-109.md) |  | P3 | Story | To Do | **Surface "Pray with the Pope" on Home as a daily session** | _New (v8 gap review)._ Correction: template **is** seeded (`tpl-pray-with-pope`, vaticannews). Not a reseed — just surface it on Home / Today's Devotions as an optional startable daily session. Relates ACTS-10. |
| [ACTS-110](../stories/ACTS-110.md) |  | P3 | Story | To Do | **Nested Templates / Template Block** | _New (v8 gap review)._ PRD v8 §10A/31C. No `template_block` kind. Reuse a whole Template inside a Session; compiler recursively expands, rejects circular nesting, bounds depth, keeps lineage. First consumer = the Rosary+Litany composite. Relates **ACTS-107**. |
| ACTS-111 |  | P4 | Story | To Do | **Explore: My Intention vs Devotion Intention split** | _New (v8 gap review; JC low pri, "not sure we need")._ PRD v8 §25A. Today `intention`+`petition` kinds; no UserIntention/DevotionIntention domain split. Explore whether the split earns its keep. |
| ACTS-112 |  | P4 | Story | To Do | **Prayer metadata: five Prayer Forms + Traditional/My origin** | _New (v8 gap review; JC low pri, "not sure needed")._ PRD v8 §25B (five forms) + §6/31A (`prayer_origin_type`, "Traditional Prayers" vs "My Prayers"). Grouped as one metadata exploration; validate the need before building. |
| ACTS-113 |  | P4 | Story | To Do | **Insights + Wisdom — future longitudinal intelligence** | _New (v8 gap review; JC "create story")._ PRD v8 §1A/3/28/32. Grounded pattern-recognition (Insights) → user-owned, user-named **Wisdom** with evidence links. Never AI-as-God's-will. Future; groups the two designed-as-a-pair concepts. |
| ACTS-114 |  | P4 | Story | To Do | **Full Audio domain** | _New (v8 gap review; JC "create story")._ PRD v8 §15–21. Multi-speaker recordings, template/session/item audio, voice-follow, full-session vs assembled, record-during-session, usage tracking. Fields stubbed only today. Future / large. |
| ACTS-115 |  | P4 | Story | To Do | **HowTo versioning + multi-source** | _New (v8 gap review; JC "keep open")._ PRD v8 §12. Auto How-To exists (ACTS-28); missing `HowToVersion` (edit imported without destroying source) + `HowToSource` (many-to-many, primary flag) + a USCCB-sourced Rosary How-To seed. |
| [ACTS-116](../stories/ACTS-116.md) |  | P3 | Story | To Do | **ACTS status dashboard + signal-ingestion workflow** | _New (JC idea, grown from the gap review)._ Living dashboard reusing the gap-artifact components JC liked — **status stat tiles** (built/partial/none/total) + **status buckets** + a **link to an editable save-back doc** — fronting a **signal-ingestion workflow**: ingest new signals → compare to what's built → classify (built/partial/not-built/**suggested**) → surface + act. Same engine as **ACTS-105** (`/prd-sync`→canon sync), generalized to a continuous feed; the path to **CRAVE** signal intake. Relates ACTS-104/105. |
| [ACTS-117](../stories/ACTS-117.md) |  | P2 | Story | **Done** | **Rebase ACTS-PRD onto v8 + apply reconcile checklist** | Shipped (`4250a1d`, `872872c`, `416a9a6`). Made **v8** the content base of `docs/ACTS-PRD.md` → **v3** (restored 1A/9A/10A, §12 seeds, 23A-B, 25A-E, 31A-C, Wisdom, clean 50-item DoD); re-applied Mission + two-part flow + Solution-Idea + "What's shipped" inventory + shipped-notes; folded in [`V8-CODE-GAP.md`](V8-CODE-GAP.md) decisions (Vessels=label, Resource Dir via Vessels, Session Purpose=free-text name, Pope IS seeded); added shipped-but-absent sections; exported `ACTS-PRD-v3.docx`. All ACs met; handoff [session-02](../stories/ACTS-117/session-02.md). Spun off ACTS-118. Relates ACTS-104. |
| [ACTS-118](../stories/ACTS-118.md) |  | P4 | Story | To Do | **Decide whether Vessels needs v8's Resource fields + external-app seeds** | _New (from ACTS-117 reconcile; JC to decide, low pri)._ PRD v8 §25D/§31A. Resource Directory is marked "complete via Vessels" — decide: keep Vessels as-is, extend with v8 resource fields (`app_store_url`, `access_model`, `best_for`…), or add a distinct Resource concept + external-app seeds (Hallow, Laudate…). Then drop the §25D open-question note. Relates ACTS-117/104/52/59/71. |
| [ACTS-119](../stories/ACTS-119.md) | ACTS-107 | P2 | Task | In Progress | **Seed — Decade of the Passion (Rosary for the Dead) + How-To** | **Seeded + verified** ([session-01](../stories/ACTS-119/session-01.md)) — ready for `/save` + `/done`. `tpl-rosary-for-the-dead`: a Sorrowful-pinned rosary of prep prayers + the "Merciful Jesus" large bead + **10 sufferings × 5 decades** + Eternal Rest, with its source and a How-To. `STORAGE_KEY` **v30→v31**. Harness 14/14, `tsc` clean, browser-verified. The name/pronoun dedication it needed was split out to **ACTS-121**. Commits `872116f`, `09d25e0`, `ceafafe`. Relates ACTS-107/110/121/28. |
| [ACTS-120](../stories/ACTS-120.md) | ACTS-107 | P2 | Task | In Progress | **Seed — Litany of the Faithful Departed + Litany of Loreto/BVM + closing collects** | **Seeded + verified** ([session-01](../stories/ACTS-120/session-01.md)) — ready for `/save` + `/done`. `tpl-litany-faithful-departed` (**52 salutations**, plural refrains) and `tpl-litany-loreto` (**50 Marian invocations**, "pray for us"), plus standalone `collect-fidelium` / `collect-departed-kindred` closing collects and their sources. `STORAGE_KEY` **v31→v32**. Harness + `tsc` clean; both render with no console errors. Name/pronoun handling → **ACTS-121**. Commits `3406b27`, `7efe55d`. Relates ACTS-107/57/121. |
| [ACTS-121](../stories/ACTS-121.md) | ACTS-107 | P2 | Story | In Progress | **Name + pronoun layer — dedicate a session "for whom"** | **Built + verified** ([session-01](../stories/ACTS-121/session-01.md)) — ready for `/save` + `/done`. `substituteDedication` in the compiler resolves the tokens `{name}` `{subj}` `{obj}` `{poss}` `{us}`, fed by an optional `SessionContext.for_whom` and a **"Prayed for"** field in the builder; read-sites strip back to generic wording when no dedication is set, and the Decade of the Passion / Eternal Rest / Faithful Departed / Loreto seeds were retrofitted with tokens. `STORAGE_KEY` **v32→v33**. Harness 21/21, `tsc` clean, **0 token leaks** in rendered output. Commits `e1410cb`, `c9655d5`. Relates ACTS-107/119/120. |
| [ACTS-122](../stories/ACTS-122.md) | ACTS-107 | P3 | Story | To Do | **Novena scaffold — generalize the block picker (chaplet slot + daily offerings)** | _ACTS-107 follow-on; **enabler**._ Generalize the "Choose the parts" swappable-block picker (`block_options`, shipped with ACTS-107) so a novena can offer **rosary + litany _or_ a chaplet** in the same slot, carry generic novena-builder naming, and support day-varying / rotating offering prayers. Unblocks **ACTS-128**. Filed `bf70c8e`. Depends **ACTS-107**; relates ACTS-110/121/128. |
| [ACTS-123](../stories/ACTS-123.md) | ACTS-107 | P3 | Story | To Do | **"Novena Prayers" pattern — day-sequenced prayers over N days** | _ACTS-107 follow-on; **enabler**._ The other novena shape (see the novena reference in PRD §6): **one prayer per day over N days**, or a different prayer each day, day-indexed as "Day N of M" — as opposed to the same prayer repeated. Decide the day-index data shape. Unblocks the **Lenten (ACTS-126)** and **Christmas (ACTS-127)** novenas. Filed `bf70c8e`. Relates ACTS-107/122/126/127. |
| [ACTS-124](../stories/ACTS-124.md) | ACTS-107 | P3 | Task | To Do | **Seed — Novena: 9-Day Sacred Heart of Jesus** | _ACTS-107 follow-on._ Reuses the already-seeded **Litany of the Sacred Heart** (`tpl-litany-sacred-heart`). Built on the swappable-block scaffold (**ACTS-122**) or the day-sequenced pattern (**ACTS-123**) depending on the source's shape. Gather public-domain wording. Filed `bf70c8e`. Relates ACTS-107/122/123. |
| [ACTS-125](../stories/ACTS-125.md) | ACTS-107 | P3 | Task | To Do | **Seed — Novena: 9-Day Mother of Perpetual Help** | _ACTS-107 follow-on._ The Redemptorist Marian novena; reuses the seeded **Litany of Loreto** (`tpl-litany-loreto`, ACTS-120). Scaffold (**ACTS-122**) or prayer-sequence (**ACTS-123**) per the source. Gather public-domain wording. Filed `bf70c8e`. Relates ACTS-107/122/123. |
| [ACTS-126](../stories/ACTS-126.md) | ACTS-107 | P3 | Task | To Do | **Seed — Novena: Lenten** | _ACTS-107 follow-on._ Day-by-day Lenten prayers/meditations, so it needs the **day-sequenced** pattern rather than the repeated-prayer shape. Pick the specific novena first, then gather wording. Filed `bf70c8e`. Depends **ACTS-123**; relates ACTS-107/122. |
| [ACTS-127](../stories/ACTS-127.md) | ACTS-107 | P3 | Task | To Do | **Seed — Novena: Christmas** | _ACTS-107 follow-on._ The **St. Andrew** Christmas novena — traditionally the *same* prayer **15× a day** from Nov 30 to Christmas, which is not the 9-day shape at all; **confirm the form with JC** before building, otherwise it rides the day-sequenced pattern. Gather wording. Depends **ACTS-123**; relates ACTS-107/122. |
| [ACTS-128](../stories/ACTS-128.md) | ACTS-107 | P3 | Task | To Do | **Seed — Novena: St. Michael (chaplet-based)** | _ACTS-107 follow-on._ A novena built on the already-seeded **Chaplet of St. Michael** (`tpl-chaplet-michael`, ACTS-11) sitting in the swappable slot — the "rosary **or** chaplet" case that **ACTS-122** exists to enable. Gather public-domain wording. Filed `bf70c8e`. Depends **ACTS-122**; relates ACTS-107/123. |
| [ACTS-129](../stories/ACTS-129.md) |  | P2 | Bug | **Done** | **Reflection icon on session rows, session detail, and vessel items** | Reflect NotebookPen was only on the Home Daily-Rosary + Word rows. Added it to Home **Continue/Today/Done** session rows (pre-links the session in Home `linkables`), the **session-detail** header (next to Share), and every **Formation `ContentRow`**. New `/reflections?link=<id>` search → `prefillLinkId`; knowledge-detail "Reflect on this" now passes it too. Browser-verified. Pending commit. Relates ACTS-102/103. |
| [ACTS-130](../stories/ACTS-130.md) |  | P3 | Bug | **Done** | **Vessel status — only completable works; sort above references** | Every non-quote item showed status pills. New `hasStatus(category)` (book/program/video/podcast) gates the pills in `ContentRow` + knowledge detail; `byStatusThenRecent` now ranks status-bearing items first, then references (article/post/quote) by recency. "General website" = article + post (JC decision). Browser-verified. Pending commit. Relates ACTS-129. |
| [ACTS-131](../stories/ACTS-131.md) |  | P2 | Task | **Done** | **Seed — Mater Dei Catholic Parish Prayer** | Seeded `mater-dei-parish-prayer` (devotional) with the full parish-card body + closing responsory (Leader "Mater Dei" / All "Lead us to your son Jesus. Amen!"); new `src-mater-dei` manual source (Diocese of San Diego, est. 2004); tags parish/community/mater dei; STORAGE_KEY v37→v38. Appears + renders in the Prayer Library. Browser-verified. Pending commit. |
| [ACTS-132](../stories/ACTS-132.md) |  | P2 | Bug | **Done** | **Session "Day N of M" counts from the session's own date** | A recurring plan's `date` rolls forward to its next occurrence as sessions finish (`finishSession`), so deriving "Day N of M" from `plan.date` showed the plan's *current* day on every session (past/future). `src/routes/session.$sessionId.tsx` now counts from `session.context.date` (the day the session was prayed), falling back to `plan.date`/`starts_on`. `tsc` clean. Shipped `d89727a`. Relates ACTS-107/99. |
| [ACTS-133](../stories/ACTS-133.md) |  | P2 | Story | **Done** | **Daily Rosary — launch an external app (Hallow) instead of the in-app session** | Some people already pray their daily rosary elsewhere and come here to journal, so the Daily Rosary slot can now **launch out** instead of starting a session. New [`src/lib/prayer/apps.ts`](../src/lib/prayer/apps.ts) catalog + a **flattened** Settings picker (In this app · Hallow · Amen · Come Pray the Rosary · Universalis · iBreviary · another app/website with a pasted URL) driven by `daily_rosary_mode` / `daily_rosary_app_id` / `daily_rosary_custom_url`, and the launch is **auto-logged as prayed** (`external_app` / `external_url` on the session, so "open again" works). Hallow's `/collections/*` URL was verified against its `apple-app-site-association` **and** `assetlinks.json` — one https URL opens the native app when installed and the web page otherwise, so there is deliberately **no separate "force web" choice**. `tsc` + browser clean. Commits `ca32634`, `6328770`, `0a7807f`, `28022f3`, `abc9c4f`. Handoff [session-01](../stories/ACTS-133/session-01.md). Relates ACTS-99/100; the setting **ACTS-153** asks for at first launch. |
| [ACTS-134](../stories/ACTS-134.md) |  | P3 | Bug | **Done** | **Vessels — A–Z sort within tiers + reseed USCCB/Hallow/Why We're Catholic** | Two Vessels-section bugs. (1) `byStatusThenRecent` (ACTS-130) buried a not-started book with old/empty `created_at` at the bottom of the status tier → replaced with `byStatusThenTitle`: status-bearing items first, then references, **A–Z by title** within each tier; removed unused `STATUS_RANK`; updated all 4 call sites. (2) USCCB/Hallow/*Why We're Catholic* are seeded but `loadDatabase()` shallow-merge lets divergent local data hide them → bumped `STORAGE_KEY` v38→v39 to force a reseed. `tsc` clean. Pending commit. Relates ACTS-130/133. |
| [ACTS-135](../stories/ACTS-135.md) |  | P2 | Story | **Done** | **Reflection organization — optional themes, no-AI tag suggestions, group-by view** | Split from ACTS-103 after a design walkthrough with JC: writing stays **effortless**, organizing happens afterward and is never required. `Reflection` gained optional `themes: string[]` (additive, **no `STORAGE_KEY` bump**) with chip + `<datalist>` entry; suggestions come from a **curated spiritual-theme lexicon plus the user's own prior tags** — deterministic, private, offline, offered as dismissible chips (**AI-suggested themes remain a future opt-in**). `/reflections` gained **Group by: Date / Theme / Source** with asc/desc sort, collapsible date-sorted sections with counts, and an "Untagged" / "No source" catch-all that sorts last. The composer can also attach a **manual web link** (new `"link"` `ReflectionLink` target carrying a `url`, scheme auto-prepended, rendered with host detail + an Open ↗). Unit harnesses run against the real code: `themes.ts` **15/15** (incl. word-boundary — no "sin" inside "using"), `inspiration.ts` **25/25**. Commits `5b70539`, `9fa7e41`, `7f48423`, `c75b6b8`. Handoff [session-01](../stories/ACTS-135/session-01.md). Relates ACTS-103/102/136. |
| [ACTS-136](../stories/ACTS-136.md) |  | P1 | Bug | **Done** | **Reflection composer — persist the in-progress draft + specific daily-readings tag** | Found browser-testing ACTS-103: there were **two separate `ReflectionComposer` instances** (Home and `/reflections`), each with its own local React state, so a draft typed on Home — body, title, links, passages — was **silently lost** on opening the journal, and vice versa; which composer held your work depended on where you started (Home icons scroll to the Home composer, while ACTS-129's icons navigate to `/reflections?link=`). Fixed with **one shared persisted draft** read/written by both, an **"in progress"** affordance on the Reflect page, and a **shared linkable builder** giving `/reflections` source parity with Home. Also: a reflection started from the daily readings is now tagged with the **actual liturgical day** (`getLiturgicalDay`, snapshotted so it stays meaningful) instead of a hardcoded "Daily Readings". Commits `a1125cc`, `cdd5fd6`, `3a8d400`, `6c67519`, `535c05c`, `99a3a88`. Handoff [session-01](../stories/ACTS-136/session-01.md). Relates ACTS-103/129/102/135. |
| [ACTS-137](../stories/ACTS-137.md) |  | P2 | Story | In Progress | **Item-level "Pin to Home" for Vessels (no favorited URL required)** | _Found finishing ACTS-136._ Home Vessels only surfaces **favorited links** (`pinnedLinks` + per-link `toggleContentLinkFavorite`), so a `KnowledgeItem` with no starred URL — a book you're reading with nothing to link out to — could never reach Home. Adds `pinned?: boolean` on the item (additive, **no `STORAGE_KEY` bump**), an item-level "Pin to Home" control, and Home rendering of URL-less pins (`PinnedLinkRow` → an internal `Link` to the detail page rather than an out-link). Decision recorded: the two toggles stay **independent**. Commits `e07207d`, `f4761e3`, `beeeccc`. Relates ACTS-136/134/130/129. |
| [ACTS-138](../stories/ACTS-138.md) |  | P2 | Story | **Done** | **Start a guided Lectio Divina from the Reflection surface** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-138/session-01.md)). A **"Reflect with Scripture"** entry card (Flame icon) on Home and `/reflections`, through the shared composer — one tap runs `startSession(LECTIO_TEMPLATE_ID)` into a fresh session. Lectio is excluded from the free-write link picker (`buildReflectionLinkables`) so a sitting can't link to itself; the card and the "or write freely below" divider collapse once writing starts. Copy: "Title or Subject", subtitle "Scripture Guided Writing or Inspired Free Writing". No new model, **no `STORAGE_KEY` bump**. Opened the follow-on arc **ACTS-139/140/141**. Commits `752a4b8`, `bb04877`, `b62164c`, `5ee6020`, `67d3817`, `08af614`, `721524e`, `eeaac04`. Relates ACTS-102/103/129. |
| [ACTS-139](../stories/ACTS-139.md) |  | P3 | Story | **Done** | **Reflections page — split into Write / Journal tabs** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-139/session-01.md)). `/reflections` split into **Write** (the composer, default) and **Journal** (entries + group/sort) on the shared shadcn `Tabs` — matching Formation / Pray / Prayers — with **no new route**. `?link=` still lands on Write pre-linked, and the group/sort/expand state plus the in-progress draft (ACTS-136) survive tab switches. Dropped the now-duplicative "Journal" heading. **No `STORAGE_KEY` bump.** Commits `005ce82`, `73049ac`, `54d3314`, plus the shared-`Tabs` restyle inside `4f1e4e9`. Relates ACTS-103/138/140/135/136. |
| [ACTS-140](../stories/ACTS-140.md) |  | P2 | Story | **Done** | **Group the journal by Lectio sitting (fold a session's movements together)** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-140/session-01.md)). A Lectio sitting now folds into **one** collapsible unit across all three lenses — `buildJournalItems` folds a session's movements (keyed by `prayer_session`, step-ordered) and `groupJournalItems` buckets the folded item, so a sitting groups under a single "Lectio Divina" source (fixing a **double-count** caused by its dual links) and a single theme. `SittingGroup` shows date · passage · *N* movements with a resume **Open** link; expanded, the pasted passage sits above the movements. Empty sittings never appear — which surfaced **ACTS-141**. No model change. Commits `4f1e4e9`, `1a2fc5a`, `96da407`. Relates ACTS-102/103/138/139/135. |
| [ACTS-141](../stories/ACTS-141.md) |  | P2 | Bug | **Done** | **Don't leave empty Lectio sessions behind when Begin is abandoned** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-141/session-01.md)). Tapping Begin created a session even if nothing was ever written, littering the journal. Empty Lectio sessions (no linked reflections **and** no completed steps) are now reaped two ways: a **load-time sweep** (`pruneEmptyLectioSessions`) and **on Close** (`leaveSession`). Chose prune over lazy-create, scoped to Lectio. ⚠️ **Gotcha worth keeping:** the first attempt pruned from an **unmount effect**, which deleted the just-opened session mid-navigation — hence the explicit on-Close hook, with the load sweep as the backstop for back/swipe. No model change. Commits `0400f2d`, `f324bb9`, `7135acd`. Relates ACTS-138/102/140. |
| [ACTS-142](../stories/ACTS-142.md) |  | P3 | Story | To Do | **Themes for Lectio journaling (tag a sitting) + lexicon gaps** | _Found closing ACTS-140._ Lectio journaling is **never themed** — `saveSessionReflection` sets no themes and the ACTS-135 lexicon only *suggests* inside free-write (it never auto-assigns), so every sitting lands under "Untagged". Let a sitting be tagged from the Journal (the ACTS-140 `SittingGroup`), suggest themes from its own text, and fill lexicon gaps ("God's will" / obedience; "Love" already exists). Storage shape TBD — decide whether themes sit on the session or on its folded reflections. Filed `54d3314`. Relates ACTS-135/140/102/103. |
| [ACTS-143](../stories/ACTS-143.md) |  | P3 | Story | **Done** | **About the app — menu entry (vision, beta purpose, "everything is local")** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-143/session-01.md)). New `/about` route (AppShell) + **About** entry (Info icon) in `secondaryNavLinks` → side rail + mobile drawer. Copy from PRD v3.1, iterated with JC: epigraph → vision (become who God calls you to be; Scripture + tradition; discernment boundary) → "more than a prayer app" (learning → reflection → insights → wisdom) → "one place for your journey" (gather scattered paper/digital; hub linking out to Hallow/Bible-in-a-Year/catechism; journaling) → beta → local-only. Name "ACTS" until the Oravia sweep. Commits `e29cf9c` (code), `346831f` (docs) — pushed; `tsc` clean. Relates ACTS-90/82/144. |
| [ACTS-144](../stories/ACTS-144.md) | ACTS-144 | P2 | Story (EPIC) | To Do | **Rebrand to "Oravia" + Marian palette (name + design system)** | _New (JC)._ Explore/adopt new brand. **Two separable tracks — (A) name ACTS→Oravia** (`Brand.tsx`, beta gate, `index.html` title, `public/manifest.webmanifest`, About) **and (B) Marian palette** (deep Marian blue + warm ivory + restrained antique gold + burgundy) — **mocked in Claude Design (`/design`)** first, then translated to central **oklch** tokens in [`src/styles.css`](../src/styles.css); verify light/dark + AA. ⚠️ **Decide first:** "ACTS" is a meaningful acronym (Adoration·Contrition·Thanksgiving·Supplication) in `Brand.tsx` + ACTS-86 — keep as tagline or retire? Relates ACTS-86/143/90. |
| [ACTS-145](../stories/ACTS-145.md) |  | P3 | Story | To Do | **Home Vessels pins — show status and link together (books, podcasts, programs)** | _New (JC)._ **Scope: Vessels content pins on Home only** — `PinnedLinkRow` ([`index.tsx`](../src/routes/index.tsx) ~L84–159) where `ownerType==="content"` and category is status-bearing (`hasStatus`: book/program/video/podcast, ACTS-130). Today the pin shows a link-out but **never the status**. Preference: **no link → show status; has a link → show both.** Reuse Formation `ContentRow`'s status pill. Not Daily Rosary; not voice/reference. Relates ACTS-130/137/129. |
| [ACTS-146](../stories/ACTS-146.md) |  | P3 | Story | Done | **Install the Archify skill (dev tooling) — audited safe** | _New (JC), parked (do later)._ Install **Archify** ([`github.com/tt-a1i/archify`](https://github.com/tt-a1i/archify), MIT) to generate architecture/workflow/sequence/dataflow/lifecycle diagrams of the app (e.g. request-flow Home→store→Supabase, share-card exports for PRD/README). **Security-reviewed 2026-09-04 via `skill-auditor` → ✅ clean:** no `pre/postinstall` hooks; single **disclosed GET-only** version-ping to a hardcoded URL (`tt-a1i.github.io/.../stable.json`, no data sent, never auto-updates); no exfil/`eval`/persistence; `spawn` uses all legit (node/git/headless-Chrome/OS-open). Decide install **scope** (global `-g` vs project) and note the `npx skills add` **installer CLI** itself was not audited. Dev-tooling only — no `src/**` change. Relates ACTS-104/105. |
| [ACTS-147](../stories/ACTS-147.md) |  | P3 | Story | To Do | **Resync PRD v3 → v4 (post-Oravia-rebrand) + fold in About-page framing** | _New (JC)._ PRD-of-record continuation. **ACTS-117** delivered **v3** (the v8 rebase = anchor/base). **Gated on ACTS-144** (Oravia rebrand) being locked + clean tree. Then `/prd-sync` v3→**v4**: refresh "What's shipped today" (Reflection↔Lectio arc 138–142, About 143, Home Vessels status+link 145), apply the **Oravia** name doc-wide, fold in the shipped **About-page copy** ("sums up this MVP") as the vision/problem anchor, resolve/carry the ACTS-117 §25D Vessels-resource-fields question, export `Oravia-PRD-v4.docx`. Depends **ACTS-144**; relates ACTS-117/104/105/143. |
| [ACTS-148](../stories/ACTS-148.md) | ACTS-144 | P2 | Story | **Done** | **Oravia Marian design system + brand mark (Claude Design → `styles.css` tokens)** | Split from **ACTS-144** (palette track). Delivered a **22-card design system** (claude.ai/design + [`docs/brand/design-system/`](../docs/brand/design-system/)) — palette, type, component looks, light+dark, all AA-verified in sRGB. **The mark shipped:** variant 5a, the *cross in the compass* — a gold ring reading as the **O**, a compass bezel and a cross (geometry is load-bearing: elongated south point, crossbar above centre). `public/` now carries `favicon.ico` (multi-res), `icon-192/512`, maskable, apple-touch + `oravia-mark.svg`, **replacing Lovable's default heart favicon** that had been shipping in the beta; two cache traps defused (`sw.js` `VERSION` `fj-v1`→`fj-v2`, `?v=2` on icon hrefs + manifest). Live palette **tuned, not replaced** — 16 tokens moved to hue 264 + antique gold so the mark sits on the published blue; surfaces deliberately untouched. Direction **Illuminated** was chosen, built, browser-verified and then **reverted at JC's call** ("launch with blue for now") — preserved as [`stories/ACTS-148/illuminated-app.patch`](../stories/ACTS-148/illuminated-app.patch) → **ACTS-151**. Findings: `.dark` tokens are unreachable (→ **ACTS-150**), `--gold` had zero component references, gold fails AA as body text (`--gold-ink` added), burgundy still has no home. No `STORAGE_KEY` bump. Commits `02cb30c`, `c81f61b`, `1d2b5b4`, `d068de7`, `54373a1`, `6c59ab0`, `f6c944e`, `d1553f8`, `6306387`, `3bcb05c`, `935c390`. Handoff [session-01](../stories/ACTS-148/session-01.md). Relates ACTS-144/90/96. |
| [ACTS-149](../stories/ACTS-149.md) | ACTS-108 | P3 | Story | To Do | **ACTS guided prayer framework — open-prayer mode + info button (A·C·T·S)** | _New (JC) — the "keep ACTS" follow-on from the **ACTS-144** rebrand._ The acronym is retired as the product name and honoured where it's true: structuring prayer. **(A)** an **info affordance** unfolding Adoration · Contrition · Thanksgiving · Supplication with a one-line gloss each (low effort, no model change); **(B)** an **ACTS open-prayer mode** — the four movements as sequenced open/free-form steps riding the `open_prayer` kind (**ACTS-108**), likely a seeded four-step template rather than a bespoke engine. Open: whether B waits on ACTS-108, where the info button lives, whether an ACTS session is captured like a Lectio sitting, and the gloss copy. Relates ACTS-144/108/86/102. |
| [ACTS-150](../stories/ACTS-150.md) | ACTS-144 | P3 | Story | To Do | **Wire dark mode (tokens exist, the `.dark` class is never applied)** | _Split out of **ACTS-148** (2026-09-04, JC: "dark ship later")._ [`src/styles.css`](../src/styles.css) has carried a complete `.dark` token block for some time, but **nothing in `src/` ever adds the `.dark` class** — grep finds only Tailwind `dark:` utilities, never a `classList` toggle or theme provider. Dark is unreachable dead code; the app renders light-only. The **values are already chosen and contrast-verified** in ACTS-148 (every dark pair clears 5.3:1), so this is the **wiring**: a system/light/dark setting persisted with the other `settings`, `.dark` applied at the root, `prefers-color-scheme` for "system", **no wrong-theme flash on first paint**, and `theme_color` following the active theme. Verify every screen incl. the `/follow` guest view. Additive settings field → no `STORAGE_KEY` bump expected. Depends **ACTS-148**; relates ACTS-144. |
| [ACTS-151](../stories/ACTS-151.md) | ACTS-144 | P3 | Story | To Do | **Ship the Illuminated palette (warm parchment, gold bands, burgundy)** | _Split out of **ACTS-148** (2026-09-04)._ Direction **chosen and fully built** — JC then chose to **launch the beta on blue**, so this story is the decision to ship it. Preserved as [`stories/ACTS-148/illuminated-app.patch`](../stories/ACTS-148/illuminated-app.patch): Illuminated `:root` + `.dark` tokens, three new `@theme inline` colours, re-tuned `--shadow-devotional`, `SectionCard` gold band + opt-in `crowned` top rule, and the Home header band. ⚠️ **The patch predates the ACTS-148 harmonisation + header-mark commits — expect a rebase, not a clean apply.** Blues and golds don't move (they shipped with ACTS-148 and already match); only **surfaces** change — cool blue-grey paper → **warm parchment**, gold section bands, crowned Word card. Decide first: **burgundy still has no home** (the mocked `rankLabel · ferialTitle` line only renders on a named celebration, while `LiturgicalDay` computes `season`/`color` every day — an always-on hook nothing uses), **Karla vs Mulish**, and whether to fold in **ACTS-150**. Re-verify AA after rebase drift; update the `published-palette`/`palette-states` cards. No `STORAGE_KEY` bump. Relates ACTS-148/150/144. |
| [ACTS-152](../stories/ACTS-152.md) | ACTS-144 | P4 | Task | **Done** | **Swap the Word and Vessels nav icons (open book, amphora)** | Nav icons that picture what they are: **Word** `Sun` → `BookOpen`, **Vessels** `Lightbulb` → `Amphora` (lucide's two-handled jar — water jug / oil vessel). One edit in [`src/components/layout/nav-links.ts`](../src/components/layout/nav-links.ts), the single definition read by the side rail, mobile drawer and bottom bar alike. `tsc` clean; both icons browser-verified in the running app (`lucide-book-open` / `lucide-amphora` in `NavSections` **and** `BottomNav`). Alternatives are recorded in the pointer — the deciding constraint is that Vessels holds **three media** (audio, books, video), so any format-literal icon (`Library`, `BookHeadphones`, `MonitorPlay`) silently drops the others; `Amphora` names no medium, echoes the section's name and is the most distinct silhouette in the nav. No `STORAGE_KEY` bump. Commits `37c0225` (code), `53159ef`, `7eef124` (docs). Relates ACTS-148/144. |
| [ACTS-153](../stories/ACTS-153.md) |  | P2 | Story | **Done** | **First-launch onboarding — ask Bible app + Daily Rosary** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-153/session-01.md)). Two questions after the beta name step, in their own `src/components/onboarding.tsx` (arrival frame extracted to `gate-shell.tsx`, since the gate is about *access* and this is about *preferences*): **your Bible** (app from `BIBLE_APPS` + translation, custom URL for "another app") and **your daily rosary** (in this app, or launch one you already use — the **ACTS-133** external path). No new capability: every field writes the same setting Settings edits, through the same `updateSettings`. **Decisions (JC):** asked **once of everyone**, current testers included — which rules out inferring "already asked" from existing data, so completion is a real field (`settings.onboarding_completed_at`, written on finish *and* skip; additive → **no `STORAGE_KEY` bump**); and **`DEFAULT_TRANSLATION` NIV → NABRE** (Catholic canon, and the translation behind the USCCB readings the Word page already links to — ⚠️ also moves anyone who never picked one; committed separately so it can be reverted alone). ACTS-144's parked brand-voice lines finally placed ("Keep your seeking for God." under **Begin**). ⚠️ **Bug found + fixed:** both steps rendered the same shapes, so React **reconciled instead of remounting** — the Radix `Select` carried over from step 1 with a stale item collection and an empty value, and **Begin** silently wrote `daily_rosary_mode: "external"` with a blank app id, the opposite of the "In this app" on screen; each step is now its own component. Lib harness **11/11**, `tsc` clean, four paths browser-verified (answer / reload / skip / Hallow) plus the `/follow` guest bypass. Commits `d926b94`, `ea441e2`, `ceed2e6`. Relates ACTS-133/144/82/143. |
| [ACTS-154](../stories/ACTS-154.md) |  | P3 | Story | **Done** | **Move the "Online Bible" link onto the Word card header** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-154/session-01.md)), commit `35d8faa`. In [`WordSection.tsx`](../src/components/home/WordSection.tsx) the link renders **inside the Daily Readings block**, smaller and directly beneath the liturgical-day link, so it reads as a **child of the readings** — when Daily Readings opens today's USCCB readings and Online Bible opens the reader's *own* Bible (`resolveBibleHomeUrl`, set in Settings and now asked at first launch). Move it to the `SectionCard` **header** `actions` slot, at a section-level weight. **Decided before filing:** keep the section named **Word** — JC weighed renaming it BIBLE and rejected it, since the card also holds the **Mass capture** (church, celebrant, homily) and **reading programs**, and naming a container after one of its contents misfiles the rest (the **ACTS-152** `Amphora` reasoning); keep the label **"Online Bible"** (placement and weight are the problem, not wording); ⚠️ **don't add a second `BookOpen`** to a header that already has one. Must stay omitted when the URL resolves to "". `WordSection` is shared, so Home **and** `/word` change together. Presentation only — no `STORAGE_KEY` bump. Relates ACTS-153/152/145. **Built as:** one exported `OnlineBibleLink` resolving `resolveBibleHomeUrl` itself and returning `null` on `""`, dropped into Home's `SectionCard actions` slot and `/word`'s `AppShell action` slot. ⚠️ **Two premises corrected while building:** `WordSection` being shared fixes only the *removal* — the header slot lives in the callers, so placement is per-surface (JC chose the per-surface option over changing the shared layout); and `/word`'s `PageShell` `AppShell` **already exposed** an `action` prop used by five other pages, so no layout change or prop threading was needed. `WordSection` no longer imports `BookOpen` at all, making the one-glyph rule structural. Verified four states (Bible set/unset × Home and `/word`): href resolves to `bible.com` with `target=_blank rel=noopener noreferrer`, exactly one such link per page, no header wrap at 375px. `tsc` + `eslint` clean. Follow-on **ACTS-155** (stale harness script found while verifying). |
| [ACTS-155](../stories/ACTS-155.md) |  | P3 | Chore | **Done** | **Repair the stale `verify-merge.ts` harness script** | ✅ Shipped ([session-01](../stories/ACTS-155/session-01.md)), commit `0c38494`. `npx tsx scripts/verify-merge.ts` died on `TypeError: mutations.addLearningItem is not a function` — its **Journey layer** block still called the pre-unification Learn API, missed when Learn/Programs/Resources were folded into the single `KnowledgeItem` entity. Ported the four dead names (`addLearningItem`→`addKnowledgeItem`, `setLearningStatus`→`setKnowledgeStatus`, `learning_items`→`knowledge_items`, `content_type`→`category`) with the **assertions preserved, not rewritten** — a baseline run (crashing block stripped) was captured first and `diff`ed, confirming the other six checks are **byte-identical**. Now prints `knowledge=5 (seeded 4, know-x=finished)` and **PASS**. ⚠️ **Root cause:** `scripts/` was absent from `tsconfig.json` `include`, so `tsc --noEmit` passed for months over a call to a function that no longer existed; added `scripts/**/*.ts`, which immediately surfaced a real `noUncheckedIndexedAccess` hole (`j.reflections[0]` unguarded). Both harness scripts green (liturgical 19/19), `tsc` clean, `vite build` clean, zero new lint errors. **Distinct from ACTS-92** (which installs Vitest/Playwright and stays deferred) — this keeps the existing harness runnable on plain `npx tsx`. Relates ACTS-92/91/154. |
| [ACTS-156](../stories/ACTS-156.md) |  | P3 | Story | **Done** | **Formatting toolbar (bold / italic / underline) in the reflection box, hideable** | ✅ Shipped + browser-verified ([session-01](../stories/ACTS-156/session-01.md)), commits `6ff9f71`, `e4d317a`, `d9abe31` — pushed. Formatting is **markdown-lite inside the same `body` string** (`**bold**`, `*italic*`, `<u>underline</u>`) — `body` stays a plain string, so **no data-shape change and no `STORAGE_KEY` bump** (store stayed on `…-db-v39`); the ACTS-94 share codec and the ACTS-136 shared draft carry formatted text without knowing about it. One shared `RichTextArea` (toolbar + textarea + ⌘/Ctrl+B/I/U) now serves **three** writing surfaces — the Home + `/reflections` composer, the journal entry's edit view, and the in-session Lectio card — and one `<FormattedText>` renders the marks on the Journal list, the sitting-group preview, the single-entry dialog and `ItemView`. It emits **React text nodes, never an HTML string**, so `<script>` in a body is displayed, not run (verified). Hide toggle persists in `settings.reflection_toolbar_hidden` (additive); shortcuts keep working while hidden. ⚠️ **The harness earned its keep:** the first parser turned "2 * 3 and 4 * 5" into italics and mangled `***x***` — which the toolbar itself produces on a second click. Fixed by matching asterisk **runs by exact length** plus CommonMark's flanking rule, so stray asterisks in prose stay literal. New unit harness `scripts/verify-richtext.ts` **33/33**; `tsc` + `vite build` clean; liturgical 19/19 and verify-merge still PASS; zero new lint errors. **Toolbar reworked after JC's first look:** the 32px bar above the field read as too much chrome, so the controls moved *inside* the textarea along its bottom edge at `size-7`/`size-3.5` — `pointer-events-none` on the strip so taps beside the buttons still reach the text, `pb-9` so a scrolled entry never runs under them, and the padding held constant so toggling never resizes the field. Hidden, only a faint corner toggle remains. **Decided while closing (JC): no voice-capture story** — dictation is left to the system keyboard mic (already works, on-device on iOS); investigation found the Web Speech API needs no cloud service, but Chrome/Safari stream audio to their own servers and Firefox lacks support. ACTS-103 keeps its voice+OCR scope; nothing split out. Relates ACTS-136/135/103/102/139. |
| [ACTS-160](../stories/ACTS-160.md) |  | P3 | Story | **Done** | **Serve the beta invitation from the app's own origin** | _New (JC), 2026-09-05._ The ACTS-159 artifact raised a question it could not answer — whether a recipient needs a Claude account to open it — so the page moved to **`public/invite.html`** (`myoravia.lovable.app/invite.html`). Files in `public/` bypass the router, so the page sits **outside the client-side beta gate**: no passcode, no name prompt, no account. Moved with `git mv`, not copied. Rewrapped as a standalone document and given its own OG/Twitter tags so sharing this link shows the ACTS-158 card. Verified 200 + no app bundle + no gate markup.  ✅ **DONE 2026-09-05** ([session-01](ACTS-160/session-01.md)) — **closed on JC's live check**; browser navigation to external domains is blocked in the Claude Code environment, so this session verified locally only (200 `text/html`, no app bundle, no gate markup, renders at 375×812). Related but deliberately separate: the invitation says "no account needed" while **ACTS-161** (stand down the passcode gate) is still To Do — JC's call is that the published state is fine as it stands and the gate comes down soon. Relates ACTS-158/159/82 |
| [ACTS-161](../stories/ACTS-161.md) |  | P2 | Chore | **Done** | **Stand down the private-beta passcode gate** | ✅ Shipped 2026-09-05 ([session-01](../stories/ACTS-161/session-01.md)), commits `bdae185` + `908a3ac`. "Remove the gate for now," so the invitation is the only thing a friend has to act on. `beta-gate.tsx` runs three steps and only step 1 gates in the exclusionary sense — and **it is already conditional**, skipped entirely when `VITE_BETA_PASSCODE` is empty, which is how we run locally every day. **Route A (recommended): clear the env var in Lovable and redeploy** — zero code, reversible in one field; "for now" says pause, not decision. Route B (delete the branch, `UNLOCK_KEY`, the `.env.example` block) means rewriting it to re-arm. It was never security — `VITE_`-prefixed, so it ships in the bundle — and the page recipients actually read (`public/invite.html`, ACTS-160) sat outside it already. Bonus: ACTS-158 caught Apple screenshotting **the gate's own name prompt** into iMessage previews. **Scope:** name prompt (step 2) and onboarding (step 3) **stay** — they personalise and configure, they don't exclude; the pointer carries a checkbox if JC wants the name prompt down too. Real accounts (ACTS-82/87/88) replace this properly. **Verified locally** with the value blanked (`.env` restored byte-identical): cleared browser lands on the **name prompt**, no code field, then onboarding, then Today. Three findings beyond a plain pass — the unlock flag `acts-beta-unlocked-v1` is **never written or read** while unarmed (so a stale flag on a tester's browser is inert, nothing to clean up); `/follow/*` never depended on the gate being armed; and **re-arming works** (restore the value, restart, the code field returns) — which is the claim the whole route rests on, so it was tested rather than asserted. ⚠️ **The actual stand-down is JC's:** clear `VITE_BETA_PASSCODE` in Lovable and redeploy — the commits only make that empty field legible (`.env.example`: it's a decision, not an accident, and your own `.env` is separate; `beta-gate.tsx`: the branch is kept **on purpose**, delete it with ACTS-82/87/88 and not before). The deployed-host acceptance box stays open until then. **Incidental find:** an armed gate costs a **blank frame** (`Splash` holds on `passcodeArmed && !checkedStorage`), so standing down makes first paint faster, not just shorter. Stale afterwards: the private tester reset guide still says a full reset re-asks for the code. Relates ACTS-82/153/158/160 |
| [ACTS-162](../stories/ACTS-162.md) |  | P2 | Story | In Progress | **Send feedback from the menu** | _New (JC), 2026-09-05._ Feedback is the point of the beta and the only channel is "text JC", which loses everything a tester notices but doesn't think worth a message. **Send feedback** joins `secondaryNavLinks` (desktop rail + mobile drawer at once) → `/feedback`: message + kind (bug/idea/other). **Channel = table, not email or Slack** — the browser can't send mail or hit a webhook without a secret, and any secret here ships in the bundle (the same reason the passcode was never security). Built like ACTS-94 **after its lockdown**: `public.feedback` with RLS on and **no policies**, revoked from anon/authenticated, granted to `service_role`, reached only through a `createServerFn` with zod validation and a hard size cap; insert-only, so no one can enumerate anyone else's. This is a **third** deliberate off-device path in a local-first app and the first carrying something the person wrote about themselves — so the UI states what leaves, and it carries **no** prayers, reflections or journey data (ACTS-157's export stays the path for those). **"Combo" is a follow-on:** a DB webhook / Edge Function → email or Slack, once rows exist; shipping the table first means nothing is lost meanwhile. ⚠️ Needs `SUPABASE_SERVICE_ROLE_KEY` locally like share links — surface the failure rather than faking success. Relates ACTS-94/157/161/82 |
| [ACTS-163](../stories/ACTS-163.md) |  | P3 | Story | To Do | **Guided tour — numbered hotspots that explain the screen** | _New (JC), 2026-09-05._ A tester's first look at Today is a screen of unfamiliar icons. An overlay they turn **on**: each explainable control wears a small numbered dot; tapping one opens a short description anchored to that control, tapping away closes it. Deliberately **not** a step-through carousel — the dot form lets someone read the two things that confused them and leave. Targets opt in via `data-tour="<id>"` on the **real element** with copy in one data file, so a renamed or moved control can't drift out of sync with its explanation the way a screenshot would. Entry: a menu item plus one post-onboarding offer, stamped like ACTS-153 so it never nags. Today first; other screens reuse the same overlay. ⚠️ The ACTS-153 same-shaped-steps React gotcha applies — key each popover by step id. **"Can Claude make a video tutorial?"** No narrated screen recording (no capture or voice tool); a silent **GIF walkthrough of the real app is possible**; an MP4 from scripted screenshots is possible but `ffmpeg` isn't installed here. Overlay still recommended: a recording is a photograph of a UI changing weekly that goes wrong **silently**, it's a heavy cellular download, and it can't be read without sound or by a screen reader. Where a GIF **does** win is *outside* the app — on the ACTS-160 invitation page, showing what Oravia is before someone has it. Relates ACTS-153/160/162 |
| [ACTS-164](../stories/ACTS-164.md) |  | P3 | Story | **Done** | **Say the "build it once, share it, pray alongside" promise on the About page** | ✅ **DONE 2026-09-05 + browser-verified** ([session-01](../stories/ACTS-164/session-01.md)), commits `d5da07d` + `3bead83`. JC: say plainly that you can build a devotion **as you have learned to pray it** — no flipping through booklets and papers — and **share it** so others pray alongside you, so someone still learning is not lost and can stay focused on the prayer. ⚠️ **The premise had to widen before the edit: this copy lives in two mirrored surfaces, not one** — `src/routes/about.tsx` (in-app, ACTS-143) and `public/invite.html` (the public invitation outside the gate, ACTS-160) both carry a near-identical "One place for your journey" section, and editing one would have re-created exactly the drift ACTS-159 existed to fix. Both changed in the same commit. **Two judgment calls:** the paragraph above already ended "…a way to sing, **and a follow link so others can pray along**" — the same promise buried mid-list, which is *why* the story existed — so the clause was **removed** rather than said twice in consecutive paragraphs; and the two surfaces keep **different contractions on purpose** (`invite.html` avoids them throughout — "and it **is** there every time" — while the in-app page keeps "it&rsquo;s"), so that is not a diff to "fix" when re-syncing. Two sentences, no new heading. ⚠️ **Verifying needed a real browser:** `/about` is client-rendered, so `curl` returns a 4 KB shell containing none of the copy — a first pass that grepped the SSR HTML proved nothing. And the preview harness could not reach the dev server: `.claude/launch.json` pins `port: 8080` while a **stale vite server from an earlier session held 8080**, pushing vite to 8081 and leaving the proxy pointing at nothing; resolved by opening the stale server directly with `preview_start {url}`. Local gate bypassed via the `acts-beta-unlocked-v1` localStorage key. Copy only — no logic, no data shape, no `STORAGE_KEY` bump. `tsc` + `vite build` clean; prettier warnings in both files are **pre-existing** (confirmed against a stashed clean tree, left untouched per ACTS-155). ⚠️ `git push` fails from this environment (no credentials) — JC pushed. **Left open:** a shared source for the two files is a real refactor (gated React route vs. standalone static file with its own OG tags), not a tidy-up; and ACTS-147 still owes the PRD a pass folding this About framing in. Relates ACTS-143/160/94/147 |
| [ACTS-165](../stories/ACTS-165.md) |  | P3 | Story | **Done** | **Swap the About sections so each holds what its heading promises** | ✅ **DONE 2026-09-05 + browser-verified** ([session-01](../stories/ACTS-165/session-01.md)), commits `d7c4c37` + `0b49207`, pushed by JC. Straight after ACTS-164, JC found the two middle About sections holding each other's content: **"More than a prayer app"** carried the faith-*learning* paragraph while **"One place for your journey"** carried all three prayer/devotion paragraphs. Swapped, and each rewritten to make its own claim. **More than a prayer app** now opens on prayer taking more than one shape and Oravia **honoring both** open and structured prayer, then states the value prop as a **contrast** rather than a category label: not that the app stores prayers, but that it **digitizes them into one place**, which is what lets you **customize a devotion the way you, your family, or your parish pray it** — compiled into a guided flow, and shareable. **One place for your journey** now carries the learning + hub paragraphs, **elevated past "good for me"** — one place is easy to hand on, so it encourages fellowship; closes on "the people walking with you are part of your journey too." **Redundancy removed:** ACTS-164 said "scattered → gathered" twice; the scattered-sources list ("a Rosary pamphlet, a hymnal, a holy card, a family novena someone texted you") is **gone for good** (JC) and the sentence tightened rather than left a stub. ⚠️ **Two good lines were nearly lost:** the guided-flow clause ("the right day, the right mysteries and readings, reflection as a first-class step, and a way to sing") and **"so the tool disappears and the prayer stays"** existed in `about.tsx` **only** — `invite.html` never had them — so a swap driven by the invite text dropped them silently. Restored, and now in **both** files. **The mirrors were not byte-identical before this story: diff them, don't assume.** ⚠️ **`find` gave a false negative** — searching the rendered page for "the tool disappears and the prayer stays" returned no matches while the sentence was on screen (it matches **truncated** accessibility-tree lines). JC saw the same absence independently. **Use `get_page_text` to confirm copy; a `find` miss is no evidence, not absence.** **"build" → "customize" + personal → communal:** JC's first wording ("the way your family or parish pray it") left the following list ("your prayers, in your order, with your intentions") pulling the other way; flagged, and JC chose naming all three. Copy only — no logic, no data shape, no `STORAGE_KEY` bump. `tsc` + `vite build` clean; prettier warnings pre-existing (ACTS-164). ⚠️ `git push` fails from this environment (no credentials) — JC pushed. ⚠️ A **concurrent session held ACTS-162 open** and edited the board + ledger at 15:56; this story's rows could not be added without also staging that status flip, so the close commit carries it, stated plainly. **Left open:** ACTS-147 (PRD resync) is further out of date — the About page's central claim changed twice today. Relates ACTS-164/143/160/147 |
| [ACTS-166](../stories/ACTS-166.md) |  | P2 | Bug | **Done** | **Make the Home daily-rosary switch actually switch — one picker for devotions and prayer apps** | ✅ **DONE 2026-09-05 + browser-verified** ([session-01](../stories/ACTS-166/session-01.md)). JC: the switch icon on the Home Daily Rosary row opens the "Daily devotion" picker, but choosing a devotion leaves the row on **Hallow**. **Cause: precedence, not a failed write.** `src/routes/index.tsx:345` decides the entire row from `daily_rosary_mode` — `"external"` means the subtitle is `Opens in ${dailyAppLabel}` and the action is the external launch link — while the picker wrote only `daily_template_id`, which **no part of that row reads in external mode**. The write always landed; the UI was reading a different setting. Settings shows no symptom only because it hides its devotion select while external. **Fix + scope, in the order JC set it:** first "make the switch switch" (choosing a devotion now also sets `daily_rosary_mode: "app"` — one line, the reported bug), then JC asked for the prayer apps **in the same list**, then to **match what Settings has**. The dialog is now **Pray here** (Standard Holy Rosary + the user's devotions) and **Open in another app** (all of `PRAYER_APPS`, Settings' order/names/blurbs, `other` included), with the checkmark bound to the **live** choice — app while external, devotion otherwise. `Another app or website` still routes to `/settings`, because a dialog has nowhere to paste a URL — JC's original "link to settings" instinct, narrowed to the one case that needs it; with a custom URL saved the row shows its domain via `dailyRosaryAppLabel`. **Verified against a real Hallow-configured profile**, not a mocked state: devotion → row left Hallow and the action became "Begin the daily rosary"; app → returned; picker reopened showed its only checkmark on Hallow; `other` → `/settings`. ⚠️ **The regression test that was missing is the follow-on** — an integration render test asserting the row leaves Hallow; blocked on the ACTS-92 harness. ⚠️ **Port-8080 gotcha recurred** (stale server holds 8080, vite → 8081, harness tab dead until repointed). ⚠️ **Hidden Browser pane**: `computer` scroll/hover time out, `screenshot`/`javascript_tool`/`get_page_text` still work. ⚠️ `stories/ACTS-162.md` was dirty **before** this session (concurrent session) and was left unstaged rather than absorbed as ACTS-165 had to. One route file; no lib logic, no data shape, no `STORAGE_KEY` bump; `tsc --noEmit` clean. ✅ **JC-verified in the app and closed 2026-09-05.** Commits `8da86e2` (code) + `74acf68` (docs) **pushed by JC**; the close commit `a265367` was pushed by JC within minutes. ⚠️ **That promptness broke something:** this session then `--amend`ed the already-pushed `a265367` into `c76380e` to fix a wrong push claim, diverging local from remote and handing JC three conflicted files on the next `git pull` (this row's file among them). **Never amend in this repo — JC pushes fast and Lovable writes straight to `main`; correct with a follow-up commit.** ⚠️ `git push` still fails here (`could not read Username`) — seeing the earlier commits on `origin/main` briefly suggested the error was cosmetic, but it is not: JC had pushed in between. Confirm with `git log @{u}..HEAD`, don't infer from the error or the remote ref. **Port 8080 is clear again** — by the time JC asked to kill the holder, nothing held it (no listener, no vite process, empty preview registry); it died with a session restart and a fresh `preview_start` bound 8080 with no fallback. The holder is always a dev server from an earlier *Claude session*, not a system service. Relates ACTS-153/99/92 |
| [ACTS-167](../stories/ACTS-167.md) |  | P2 | Bug | **In Progress** | **Make the app-icon mark fill its tile — it shipped at 40% and reads as a dot** | 🔧 **In progress — code complete, awaiting JC's device check.** _New (JC), filed 2026-09-06._ JC screenshotted his iPhone home screen: the Oravia mark sits visibly smaller in its tile than the marks beside it. **Two causes compounding.** (1) The mark's 100-unit canvas has air baked in — the ring is `r33`, so the visible mark is only **66 of 100 units**. ACTS-148 scaled that *whole canvas* to the tile, landing the mark at **40% of the tile** (72px of 180; 202 of 512) and the maskable at **30.5%**. Nothing looks wrong in isolation — only next to other apps. (2) `apple-touch-icon.png` had **transparent corners**; iOS applies its own squircle and composites transparency onto black, so the navy tile was inset too. **Fix: size the tile from the ring, not the canvas** — `canvas = tile × ring_frac × 100 / (2r)`, with `ring_frac` **0.66** for the plain icons (which *is* the brand's own clear-space rule — a quarter-diameter each side — so the mark goes as large as the guideline allows and no larger) and **0.62** for the maskable (inside Android's 80% safe circle; the mark is circular, so nothing crops). `apple-touch-icon` + maskable now full-bleed; the `purpose:"any"` PNGs keep 20% corners. `favicon.ico` gained the documented **16/24/32/48/64** frames (it had only a 16), small cut below 20px. **New `docs/brand/make_icons.py`** — the ACTS-148 generator was never committed, so the icons had no reproducible source; it writes both `public/` and the design-system assets and resolves from `__file__` (its siblings all assume `cwd = docs/brand`; `make_og.py` in fact **dies** from the repo root — pre-existing, `og-cover.png` untouched). ⚠️ **Icons don't ship by replacing files** — `sw.js` serves images stale-while-revalidate and precaches `/icon-192.png`, so `VERSION` `fj-v2` → **`fj-v3`** and every `?v=` → `v=3` (`__root.tsx`, `manifest.webmanifest`, `invite.html`, the shell list). Same trap as ACTS-148. Verified by **pixel measurement** (ring/tile ratio, centring, corner alpha, `.ico` frames) + an iOS-squircle before/after render; `tsc --noEmit` clean; `make_about.py`/`make_flyer.py` re-run clean against the refactored `mark.py`. ⚠️ Forked off an **ACTS-162** chat — `ACTS-162.md`, `about.tsx`, `0003_feedback.sql` were dirty beforehand and are **not** staged; `invite.html` held both that session's copy edit and this `?v=` bump, so **only the `?v=` hunk is committed**. **Left open:** JC confirms on his own home screen after publish — the one check that can't be done from here. Commits `164a522` (code) + `e8b9334` (docs) — **committed, not pushed** (`git push` fails here: `could not read Username`), so JC pushes. Relates ACTS-148/144/96 |
| [ACTS-168](../stories/ACTS-168.md) |  | P3 | Story | **In Progress** | **The mark as the O — a wordmark lockup, built and wired but switched off** | 🔧 **In progress — built, wired, and switched OFF; JC's decision is the only thing left.** _New (JC), filed 2026-09-06._ JC asked for the mark set **inside** the word as its O (it was drawn as "the O of Oravia" in ACTS-148), then: do both call sites, keep the design-system card **proposed**, sync it, **but do not render it in the app yet**. So: `OraviaWordmark.tsx` carries the lockup plus one flag, `WORDMARK_LOCKUP_ENABLED`, shipping **`false`**; `Brand.tsx` (mobile header + desktop side rail + mobile drawer, three renders) and `gate-shell.tsx` (beta gate, name prompt, both onboarding questions) each branch on it, so one line switches every surface together. **`gate-shell` moved with `Brand` deliberately** — it is the *largest* the name appears anywhere, the first thing a new person sees, and it has **never carried the mark at all**, so switching only the header would leave the biggest wordmark plain; turning the flag on **adds** the mark there rather than rearranging it. **Two constants were measured, not guessed:** `O_INK = 0.648` (a Cormorant capital O inks 0.648 of its font-size — rasterised and bounding-boxed at 128 and 256px, identical at weights 400/500/600; below ~40px the measurement is rasterisation noise, hence a constant not a runtime probe) and `OUTER = 72` (**SVG centres a stroke on its path**, so the visible outer diameter is `2×(r+stroke/2)` = 72 of the 100-unit canvas, **not** `2r` = 66 — the same off-by-a-stroke as ACTS-167, which here renders the ring ~9% small). Cross paths extracted to `REGULAR_CROSS`/`SMALL_CROSS` on `OraviaMark.tsx` so the wordmark cannot drift from the mark. Specimen card at `brand/wordmark.html` — fit ladder, cross vs ring-only, colourways, tagline lockup, small-size ladder, side-by-side with today's header, do/don't rules — marked **Proposed, not adopted**, the same convention `palette-states.html` uses for the staged palette. ⚠️ **Its `@dsCard` marker was first written `section`/`title`/`order` instead of `group`/`name`/`subtitle` and would have been silently skipped by the indexer** — check the marker against a working card, the failure is invisible. **Synced to claude.ai/design** (Oravia Design System, 23 cards) along with ACTS-167's corrected `mark.html` and regenerated icons, which had been sitting at the 2026-09-04 version. ✅ Verified both ways against the running dev server: flag off → plain `Oravia` h1, zero SVG, header unchanged; flag on → both surfaces switch, ring 22.0px measured against 22.0 expected at 36px, mark resolves `--gold`, word `--foreground`, and the gate `h1` still computes an accessible name of **"Oravia"** (role=img + aria-label) rather than "ravia". `tsc --noEmit` clean both ways. ⚠️ **The integration test that matters** is the one asserting the mark SVG is absent while the flag is off — that is what stops this shipping by accident; blocked on the ACTS-92 harness. ✅ **JC reviewed all four surfaces and said on** — flag flipped in `266a008`. ⚠️ **The one real tradeoff, recorded at the flag:** as a standalone mark the header ring was **21.6px** (a 30px `OraviaMark` beside 20px text, 1.5× the type, free to be any size); bound to the type as its O it is **13.3px** at the same font-size — **62% of what it was**. Inherent to the idea, not a defect. If the header needs its presence back, raise that surface's `fontSize`, **do not touch `FIT`** — the ratio is what keeps the ring reading as a letter rather than an ornament beside one. The arrival screen runs the other way: it gains a mark it never had, at 36px. **Header bumped to 24px** (`LOCKUP_SIZE`, `21d41c6`): 20px put the ring at 13.3px against the standalone mark's 21.6px (62%); 24px gives **16.0px, 74%** — as far as the row goes before the tagline stops sitting comfortably. Verified at 375px: right edge 204 of 375, no overflow, no collision with the date. **Left open:** JC sees it in the real app after publish. Commits `789f055` (code) + `b15548a` (docs) — **committed, not pushed** (`git push` fails here: `could not read Username`), so JC pushes. Relates ACTS-148/167/144 |
| [ACTS-159](../stories/ACTS-159.md) |  | P4 | Story | **Done** | **Refresh or retire the published Oravia brand artifact** | _Found while shipping ACTS-158; JC: capture, don't fix now._ The published brand/About artifact (<https://claude.ai/code/artifact/03d0869e-d9ac-40a3-b7ba-ce903b54de62>, source `docs/brand/oravia-brand.html`) still draws the **old flame-and-open-O mark** rather than the ACTS-148 cross-in-compass, carries **no beta link**, and its copy predates the later About trims. Private + hand-shared only, so the risk is a link already sent to a tester showing the old identity. Open: refresh (same URL, so existing links resolve) vs. retire, now that the two PNGs and the ACTS-158 share card cover the same job.  **DECIDED 2026-09-05: RETIRE, not refresh.** A replacement was built and published while closing ACTS-158 — **<https://claude.ai/code/artifact/0a832e6c-efa7-4cea-91bb-5503fee85a41>** ("Oravia Beta Invitation", 🧭; source [`docs/brand/oravia-about.html`](../docs/brand/oravia-about.html)) — the web twin of `oravia-about.png` with the current mark, current copy, the palette lifted verbatim from `make_about.py`, and the beta link as a real tappable `<a>`. **HTML over .docx** because the piece is sent to phones: it reflows (the 1600px PNG needs pinch-zoom), the text is selectable and screen-reader accessible, the mark is inline SVG, and a `@media print` block makes ⌘P → Save as PDF produce the file version, so the docx need never exist. Retiring beat refreshing in place because the old artifact was private and hand-shared only (no public link breaks), and refreshing would have meant hand-maintaining a second HTML page with the same copy — the exact drift that created this story. **Left to settle:** `docs/brand/oravia-brand.html`, the retired artifact's source, is still in the repo carrying the old mark and pre-trim copy — delete or mark superseded in place. README now flags it RETIRED.  ✅ **DONE 2026-09-05** ([session-01](ACTS-159/session-01.md)), commits `9023026`, `503e3d3`, `7a24e12`, `dd6c398`. `oravia-brand.html` **deleted** (JC), dangling refs in `make_og.py` + README cleaned up; `design-system/brand/mark-at-size.html` still cites it and is left as the historical record of why the flame was retired. ⚠️ **Raised at close, not in scope:** where a recipient actually reaches a shared `claude.ai/code/artifact/…` link, and whether it needs a Claude account — if so, the page belongs on the app's own origin (a static file in `public/`, outside the client-side beta gate) instead. Relates ACTS-144/148/158 |
| [ACTS-158](../stories/ACTS-158.md) |  | P3 | Story | **Done** | **Social share preview card (Open Graph / Twitter) for the beta link** | _New (JC), filed 2026-09-05._ Sharing the beta link produced **no card**: nothing set `og:image`, so **Apple screenshots the page instead** — and caught the beta gate's name prompt, too small to read. New 1200×630 card at `public/og-cover.png` (cross-in-compass mark, name, tagline, one about line, **"Free beta · No account needed · Your entries stay on your device"**) on the dark Marian-blue/gold palette, generated by `docs/brand/make_og.py`. Tags added to the **root** route so every page inherits the card; child heads still override title/description by key. Verified against the **server-rendered** HTML on `/`, `/about`, `/pray`, `/settings`. ⚠️ Two real bugs caught while building: copying the supabase client's `import.meta.env || process.env` pattern **blanks the app** in the browser (that file's `VITE_*` is always set so its `process` arm never runs; `VITE_PUBLIC_URL` is normally unset, and `head()` runs on every client navigation) — now guarded; and the **index route's own `og:title`/`og:description` overrode the root's**, so the shared `/` URL still read "Your daily prayer companion" and silently dropped the beta/no-account line. Home now carries the tagline as its share headline (JC) and inherits the root blurb. `tsc` + `eslint` + `vite build` clean. Next: deploy + re-share (scrapers cache; `?v=1` bumps on redraw).  ✅ **DONE 2026-09-05 — confirmed live by JC** ([session-01](ACTS-158/session-01.md)). Grew past the original scope as JC reviewed: **"calm" removed** from every brand surface (card, `og:description`, root + Home descriptions, manifest) while deliberately kept in `seed.ts` (a Nativity reflection) and `themes.ts` (a **keyword** in the peace matcher — deleting it would break theme matching); the **dead `www.` host** corrected in code and on the printed collateral; the **current mark** put on the About card (which had none) and the flyer (still drawing the retired flame-and-open-O), extracted to one shared `docs/brand/mark.py` — `og-cover.png` regenerates **byte-identical** after the extraction; **no CTA on the card** (JC: "it works as it is"). ⚠️ **Second bug JC's tagline note exposed:** the index route's own `og:title`/`og:description` overrode the root's, so `/` — the URL people actually share — still read "Your daily prayer companion" and **silently dropped the "free beta, no account needed" line**, the whole point of the card. Relates ACTS-144/148/82 |
| [ACTS-157](../stories/ACTS-157.md) |  | P2 | Story | **Done** | **Export my journey for AI insights — menu item, built-in prompt, date range** | _New (JC)._ The app has **no export**; the whole journey lives in the local `Database` ([`types.ts:827`](../src/lib/prayer/types.ts)) and can only be read a screen at a time. New **secondary nav entry** (side rail + mobile drawer, per ACTS-143) → an export screen producing **one Markdown document** to paste into ChatGPT/Claude. **Locked with JC at filing:** all four layers exported (reflections/journal with ACTS-135 themes and resolved links, prayer sessions incl. external-app logs, intentions + Mass experiences, formation/knowledge items); **Download `.md` and Copy to clipboard** both; range **Everything (start → today)** (default) or **custom from/to**; Markdown chosen because both assistants read it best and it stays human-readable. The **prompt is written into the top of the document** — what the data is, what to look for, and the PRD §1A/3/28/32 guardrail that it must **name patterns and ask questions, never claim to speak for God or hand down discernment**. Read-only, no `STORAGE_KEY` bump; ACTS-156's markdown-lite marks are already valid markdown and pass through untouched. ⚠️ **Distinct from ACTS-113** (in-app grounded Insights → user-named Wisdom, which stays open as the future longitudinal feature) — this is the near-term path that needs no AI in the product. **Open:** prompt wording (iterate with JC, possibly editable on-screen); which date key filters each entity (`MassExperience.date` vs `created_at`; a `KnowledgeItem`'s `created_at` is when it was *saved*, not read); oversize journals vs. chat-context limits (show size, warn, maybe a trimmed variant); photos/audio can't be exported as text and must be said, not silently dropped; and the **privacy line** — About promises "everything is local", so the screen must state that the file is built on-device and that pasting it into an AI service sends it there. Relates ACTS-113/135/103/102/143/82. ✅ **DONE 2026-09-04 + browser-verified** ([session-01](../stories/ACTS-157/session-01.md)), commits `8c85d7e`, `8521c31` — pushed. New `/export` route + **Export journey** nav entry (`FileDown`, next to Add prayers) → one Markdown document built by `src/lib/prayer/journeyExport.ts` (pure over `db`, read-only, **no `STORAGE_KEY` bump**). Four layers filtered by range, sections oldest→newest, links resolved through the existing `resolveInspiration` (ACTS-102/135) so no raw ids leak; ACTS-156 markdown-lite marks pass through untouched. **The prompt is editable in the app** (collapsed section + Reset), so its wording can be iterated by use. **Date key decided per layer and stated in the file**: Mass by its own `date`, a saved item by `start_date` else `created_at` (= when saved, not read). Size is shown ("About N words") and warned past 200k chars rather than capped; photos/audio/common-prayer-texts are **named as excluded, never silently dropped**; the privacy line sits right above the buttons — built on-device, but pasting hands it to that company's servers. ⚠️ **Found while verifying:** `quoteBody()` falls back to `title`, so every **book** printed its own title back as a blockquote — guarded with `isQuote()`; a quote's heading was also a 57-char snippet of the text below it, now just "Quote". New harness `scripts/verify-journey-export.ts` **47/47**; `tsc` + `vite build` clean; other three harnesses still green; zero lint errors. **Copy to clipboard needs a human tap** — the pane can't focus the document for `navigator.clipboard`; the download blob was captured and read back instead. **+ "Since last export" (JC, same session):** a third range option — everything since the export the user actually **took**, stamped in `settings.last_export_at` by Copy/Download only (never by opening the page), hidden until there is one, and leading by default when there is. The window opens **on** the day of that export, not the day after: ranges are date-granular, so exporting Thursday morning and journaling Thursday night would otherwise lose the entry — overlapping the day re-sends a few at most, and the screen says so. ⚠️ **Building it exposed a real bug in the first cut:** the stamp is UTC (`toISOString()`, like every `created_at` here) and the builder filed entries by `iso.slice(0,10)`, so an evening export opened the catch-up window **in the future** — the exact gap the overlap prevents. Underneath sat the general case: **every** layer was filed by its UTC day, so an 8pm-Pacific reflection filed under tomorrow, outside a range the user picked ending that day. `dayOf()` now converts to the **local** calendar day and leaves an already-plain date (`MassExperience.date`, `start_date`) as written instead of re-parsing it as UTC midnight — the trap `todayISO()` documents in `compiler.ts`. Harness pins `TZ`; `last_export_at` is additive, still no `STORAGE_KEY` bump. **+ anchored to the user's own entries (JC):** with an empty journal the label read **"Everything — 2023-12-31 to today"** — every seeded row carries `now = "2024-01-01T00:00:00.000Z"` (UTC midnight → Dec 31 Pacific), so a new user was told their journey began before they installed the app. That constant is now exported as **`SEED_EPOCH`** (one sentinel across all 48 pre-installed records; a real `new Date().toISOString()` can never equal it, so no id-prefix guessing) and `journeyDateBounds` counts **only the user's own rows** — an untouched install now reads "Everything — Your whole journey". Seeded library items are **kept, not dropped** (someone may really be working through the pre-installed Bible in a Year) but are **headed by name alone** — their only date is the install stamp — and **flagged "came with the app, not chosen"**, so an assistant can't read them as evidence of what this person was drawn to. Harness now **57/57**. **Left to try (not blocking):** one human tap on Copy (the pane can't focus the document for `navigator.clipboard`), JC's pass on the prompt wording — editable in the app precisely so it can be judged in use — and a real export actually run through an assistant. Whatever shape that reveals feeds **ACTS-113**, which this story deliberately left open. |

---

## Process

How the ACTS backlog is numbered and kept current — so this doesn't drift:

1. **Numbering.** Every completed unit of work gets one `ACTS-NN` row, numbered
   oldest-first by the commit that delivered it. Numbers are permanent — they never
   get renumbered when new rows are inserted; new work always takes the next number.
   Open/not-yet-started stories are also numbered so they're easy to reference when
   starting a chat (`/start ACTS-NN`); their id is reserved until the work is done.
2. **Commits.** Each row logs the commit hash(es) that delivered it. A row can carry
   several commits (implementation + fixups + its `docs:` handoff).
3. **EPIC column.** Left blank on purpose — JC fills it in to group rows into epics.
   The section headers above are a suggested grouping, not the epic assignment.
4. **The counter.** [`stories/.counter`](../stories/.counter) holds the last-used
   number (currently **168**). The next brand-new story is ACTS-169.
5. **One chat = one story.** Each working session is tied to a single ACTS story;
   its handoff lives at `stories/ACTS-NN/session-NN.md` (see
   [`stories/_TEMPLATE.md`](../stories/_TEMPLATE.md)). The compact active board is
   [`stories/README.md`](../stories/README.md); this file is the full ledger.
6. **No Jira.** Per [`.claude/workflow.json`](../.claude/workflow.json) the tracker is
   `none` — this repo is the tracker. Follow `~/.claude/commands/WORKFLOW.md` but skip
   every Jira/sync step.
7. **Testing — applies to every story** (convention: **ACTS-91**, Done). Each code-change
   pointer **documents** tests in a **Tests** section (unit · integration · E2E), enforced by
   [`stories/_TEMPLATE.md`](../stories/_TEMPLATE.md), **and** tracks the testing work as a task.
   The shared E2E flow catalog is [`docs/E2E-TEST-PLAN.md`](E2E-TEST-PLAN.md). No runner is
   wired yet — the harness build is deferred to **ACTS-92**, so coverage stays **planned**
   until it lands. **Historical Done rows (ACTS-01…75)** aren't retrofitted one-by-one; their
   shipped features are covered collectively by the E2E flow catalog (E1–E15). New/active
   stories fill in their own Tests section going forward.

_This ledger was generated 2026-08-25 from `git log` + the prior gap-merge backlog and
`HANDOFF.md`. See `HANDOFF.md` for run/architecture context._
